Editar
Atividades Oficinas Spot 01 08 26
Atividades Oficinas Spot 03 08 26(1)
Você, eu vou te mandar um print da minha, que eu tenho um filtro lá no OS, e acho que eu consigo ver as ordens que estão sem ter um aceite da OSG. Ou seja, eles estão disponíveis lá, mas por algum motivo não entraram na agenda do técnico, né? E aí, e ele também das que estão, eles não aceitaram, né? Que eles não clicaram no botão lá para aceitar. É possível ver por esses chamados aí que eu vou te mandar agora, com base na planilha, se existe alguma, algum detalhe nessas ordens que chama atenção para a gente classificar elas como sem aceite?
seg., 3 de ago. às 13:37
Se você perceber, tá, criei o arquivo e criei um texto. Aí do lado da descrição fica requisito.
Atividades Oficinas Spot 03 08 26(2)
ofc2026.ods
Arquivo
o q é esse nan
Atividades Oficinas Spot 04 08 26
Atividades Oficinas Spot 03 08 26 (1)
esta assim o painel 
Texto colado(2).txt
Documento
app(1).py
Python
copiar o prompt que tá lá, aí você só altera para incluir a programação correta dentro do prompt que já existe.
app_com_indicadores(1).py
Python
ter., 4 de ago. às 17:53
qua., 5 de ago. às 11:01
TA DANDO ERRO 
vamos do inicio
app_visao_diaria(1).py
Python
Eu atualizei a página sem quere será q vai da problema?!
O painel perdeu as informações que pedi antes as regiões o mci perdeu tudo 
Esse ?
app (1).py
Python
requirements (1).txt
Documento
corrigo os codigos
??
n tem o supabase
veja se ta tudo ok tmb
qui., 6 de ago. às 9:04
Texto colado(4).txt
Documento
leia ele veja o que falta e corrigi
vamos corrigir o codigo 
qui., 6 de ago. às 11:23
O QUE SIGINIFICA HOSPEDADO NO SISTEM ASTREAMLIMIT
Texto colado(5).txt
Documento
o da oficina foi 
essa visão geral traz somente o dia selecionado ou o consolidado 
explique esses indices
Atividades Oficinas Spot 05 08 26 (1)
quantas ordes improdutivas de manunteção existem nesse arquivo
Atividades Oficinas Spot 08 08 26 10 08 26
porque ao salvar n altera a oficina?
es certo esse layout]?
coinsegue ver que tem improdutivas n contadas ~?
me ajuda agora nesse caso 
da ordem final 3886
Atividades Oficinas Spot 03 08 26(3)
Atividades Oficinas Spot 03 08 26 (1)(1)
Atividades Oficinas Spot 04 08 26(1)
Atividades Oficinas Spot 04 08 26 (1)
ainda faltam so um momento 
Atividades Oficinas Spot 05 08 26
Atividades Oficinas Spot 05 08 26 (2)
ter., 11 de ago. às 9:33
os_improdutivas.xlsx
Planilha
vamos refinar a anlise de improdutivas
Atividades Oficinas Spot 10 08 26
tem o campo de motivo de improdutva e a observação do tecnico
aonde?
agora foi
ta assim n fiz nenhum alteração
ihuu
sem erros visiveis
eu ainda não fiz a correção do codigo do painel principal me envia denovo o ultimo codigo do portal de gestão
Acaaa83a E91c 472d A222 7d3353a509df
3308a3c4 482b 46fe Ae3f 7e19233b7791
o ofs ta dizendo que ta executada e no sistema diz q foi no show
no portal da oficina o follow funciona, mas n estou o botão para ela responder
antes de replicar vaoms fazeruma ajuste, em como ser separado por OS e não agrupada? Porque pode ser que cada caso seja um se for separado seria melho
Atividades Oficinas Spot 12 08 26
no meu painel eu tive um caso q acho q a interpretação n foi correta, pq pra mim nesse relato é o tecnico q esta dizendo que teve problema e não o cliente
Ó, te mandei um print. Desculpa, te corta. Mandei um print. É... E essa é uma aba interna da ordem. Caso eu tenho... Quando eu listo ela, determinadamente, especificamente aquela OS, Quando eu entro nela tem uma aba de histórico que... tem todas aí... Tem, tem um log lá, né, de base que o OFS carrega
qui., 13 de ago. às 14:54
feito
 mas puxa a sm 
agora sim 
sera q precisa mudar a url?
Atividades Oficinas Spot 13 08 26 15 08 26
Atividades Oficinas Spot 13 08 26
Texto colado(6).txt
Documento
d2706bd6-dc85-4d28-8a42-27abab1087ea.py
Python
ESSA foi a ultima versão que instalei que vc me mandou, prefiro que vc corrija com base nela
eu acho que como oficinas de outros consultores podem fazer ordens em outras regiões isso pode bugar o numero?
a execução bateu
Atividades Oficinas Spot 13 08 26 (1)
Atividades Oficinas Spot 13 08 26 15 08 26(1)
vou importar os csv novamnte pra vc 
 da onde vem essas 5 ordens?
Descobri que tem alguma coisa errada realmente no código, porque desde o dia 8, se eu selecionar, no dia 7, no dia 7, pera aí só um momento, tá? Deixa eu carregar aqui. Dia 7, dia 8. A partir do dia 8 até o dia 12, o painel está simplesmente zerado, só tá contando OS extras, executada extra. Eu vou mandar um...
A7333237 65ae 406d 9f2f 01d07bc5788e
Atividades Oficinas Spot 09 08 26 11 08 26
Atividades Oficinas Spot 10 08 26 12 08 26
Aed1deb6 0e62 4ef1 9ec7 481fcd79f255
1938e593 80ff 4f22 8ab2 1a3f54744bc1
sáb., 15 de ago. às 8:06
bom dia Hakim
Atividades Oficinas Spot 14 08 26 16 08 26
Atividades Oficinas Spot 14 08 26 (1)
ok examini como estaq no codigo atual o ultim o app.py foi esse que vc me mandou  
FS - MCI E MEDIDAS DE DIREÇÃO - Google Planilhas.pdf
PDF
analisa esse pdf do paulo da ultima reunião da quinta

Analisei. Esse PDF é bem útil porque ele mostra que o Paulo está tentando transformar MD em uma ferramenta de gestão de causa, e não apenas em um percentual.

Na única página, ele organiza uma matriz com Tipo → Razão → “Contabiliza M” → Ação Interna → Ação Externa → Melhoria. A parte mais importante para o nosso painel está justamente na coluna “Contabiliza M”.

Para Improdutiva, aparecem pelo menos estas regras:

Veículo indisponível no local → SIM. E ele propõe ações como avaliar recorrência, envolver CX/CCO/CC, verificar se a oficina confirmou disponibilidade previamente e ajustar melhores horários com o cliente.
Problemas com sistemas → NÃO. A ação é abrir chamado interno para correção/entendimento.
Problemas técnicos com veículos → NÃO. Aqui ele inclusive questiona ações adicionais e sugere verificar a qualidade do apontamento da oficina e a descrição do problema.
Técnico não liberado na portaria → SIM.
NO-SHOW → SIM.

E isso me chama bastante atenção em relação ao que construímos.

Nossa MD hoje é mais “bruta” que a concepção dessa planilha do Paulo. No nosso código, somamos todas as classificações Improdutiva agendada + Improdutiva extra no numerador da MD. Depois dividimos isso por executadas agendadas + extras + todas essas improdutivas.

Mas a planilha do Paulo indica que nem toda improdutiva deveria necessariamente penalizar a medida de direção.

Esse é um ponto maior do que a alteração de MCI que acabamos de fazer.

Por exemplo: imagine 10 improdutivas, sendo 4 por veículo indisponível, 3 por problema de sistema e 3 por problema técnico do veículo.

Nosso painel hoje considera:

10 improdutivas na MD.

Pela matriz do Paulo, aparentemente entrariam:

4 na medida, porque veículo indisponível = SIM; as 3 de sistema e as 3 de problema técnico = NÃO.

Isso poderia explicar muito bem por que você vinha sentindo que a sua MD estava alta.

E tem outro detalhe interessante: o Paulo marca NO-SHOW como SIM. No nosso painel, no-show possui indicador próprio e não entra atualmente no numerador da MD. Nossa fórmula de MD considera apenas as classificações improdutivas.

Então eu não mudaria mais nada amanhã antes de conversar com o time. A mudança que fizemos na MCI continua conceitualmente coerente: execução extra passa a contar como execução, mas continua transparente como extra. O PDF não contradiz isso.

O que eu levaria para a reunião é uma pergunta bem específica:

“Na MD oficial, quais motivos de improdutividade entram no indicador? E o no-show entra na MD ou é acompanhado apenas como indicador separado?”

Porque esse PDF sugere que existe uma MD gerenciável, na qual só determinadas causas contabilizam. Se o Paulo confirmar isso, nossa próxima evolução fica muito boa: em vez de simplesmente dizer “Improdutiva = ruim”, o painel passa a distinguir improdutividade que contabiliza MD de improdutividade que não contabiliza MD, mantendo ambas visíveis.

Aí sim o nosso painel passa a falar exatamente a mesma língua da gestão, mas com muito mais transparência sobre o que está por trás do número.

dom., 16 de ago. às 12:06
otimo pode fazer essas letras ficaram grandes se puder diminuir para ficar mais estetico
eu entendi as primeiras imgaens mas o ranking fiquei na duvida
aqui
Atividades Oficinas Spot 17 08 26 23 08 26
esse é o arquivo original e hj
entendi tem varias bases separadas 
vai ficar com essas bases 
Atividades Oficinas Spot 16 08 26
seg., 17 de ago. às 11:04
tem relação com nosso painel ou é geral
seg., 17 de ago. às 15:16
aguardo ainda 
Atividades Oficinas Spot 17 08 26 23 08 26 (1)
Atividades Oficinas Spot 18 08 26 24 08 26
compare com a foto de hj 
Atividades Oficinas Spot 01 08 26(1)
Atividades Oficinas Spot 02 08 26
Atividades Oficinas Spot 03 08 26 (4)
perai q ja mando o restante
Atividades Oficinas Spot 08 08 26
Atividades Oficinas Spot 07 08 26
Atividades Oficinas Spot 06 08 26
Atividades Oficinas Spot 05 08 26(1)
Atividades Oficinas Spot 04 08 26(2)
Atividades Oficinas Spot 13 08 26(1)
Atividades Oficinas Spot 12 08 26(1)
Atividades Oficinas Spot 11 08 26
Atividades Oficinas Spot 10 08 26(1)
Atividades Oficinas Spot 09 08 26
Atividades Oficinas Spot 14 08 26 (1)(1)
Atividades Oficinas Spot 18 08 26
Atividades Oficinas Spot 17 08 26
Atividades Oficinas Spot 16 08 26 (1)
Atividades Oficinas Spot 16 08 26(1)
Atividades Oficinas Spot 15 08 26
lista de oficinas 2026 (2).xlsx
Planilha
2026 08 18T18 29 Exportllll
 Essa lista aqui, essa lista que tá no XLS, é a lista do SAVE, que tem os endereços, CEP e tudo mais. A segunda lista, que é a que tá em CSV, é a lista que eu importei, baixei do nosso painel, o que tá cadastrado lá no nosso painel de atividades, inclusive que são os nomes vigentes hoje. Mas, aí você confronta as duas, né? Basicamente os nomes vão estar parecidos, mas o nome da oficina atual que a gente tá trabalhando é o que tá no arquivo CSV, e os endereços dessas oficinas, aí você pode fazer o check, é o que tá no arquivo XLS.
No caso aí, a Miguel Ângelo, São Bórgio, a gente não vai contabilizar, e sim a JF Instalações de Passo Fundo, a Vestec Conecta, a outra pode descartar. E aí talvez esse endereço, inclusive a latitude e longitude, certo?
ate tentei salvar os arquivos nos formatos mas mesmo assim fica dando erro
que espetaculo...
Eu gostaria que você fizesse diferente, assim. Por exemplo, a região Norte, ela tem o desenho dela, né? Separado os estados, né? O Amazonas, Roraima, Acre, Rondônia, Pará. Então, tem os estados separados, né? E por mais que você tenha colocado as cores ali das oficinas, né? Vamos fazer assim. A gente vai assumir. Quando for cliente, a gente vai assumir a cor vermelha, né? Para dizer tudo que for atividade de serviço, certo? As oficinas, todas as oficinas, elas vão ter aquele formato ali de, acho que é um balãozinho, um losango, não sei. A gente vai assumir a cor azul. Todas as oficinas terão esse formato, né? E um pouquinho maior, um pouquinho maior o tamanho ali delas, acho que alguns milímetros maior, só para dar um destaque, e elas forem azul, tá? Aí, cada região você faz em uma cor diferente. Por exemplo, a região Norte, que é a do Fábio, vai ter o desenho delimitado de cada estado dele lá. Vamos colocar a cor cinza, né? Para que o azul e o vermelho consigam aparecer lá dentro. O Nordeste a gente pode colocar um verde claro, né? E com os estados deles lá dentro, né? Maranhão, Piauí, Bahia, Pernambuco, todos delimitadinhos, né? Com as atividades lá dentro, né? Com as nuvens de atividades lá. E aí, Minas Gerais a gente pode colocar um azul clarinho, né? Para destacar ele ali, que é mais uma região do Fábio. A região da Glesse, que é a região Centro-Oeste, ali tem Tocantins, Goiás, Distrito Federal, Mato Grosso, Mato Grosso do Sul, você pode colocar um amarelo claro para que ela se destaque. A região Sul, você pode colocar um lilás claro. Paraná, pode colocar um tom de laranja, né? Claro. E São Paulo você pode colocar... Deixa eu ver uma cor para São Paulo aqui. São Paulo, Rio de Janeiro e Espírito Santo. Talvez um outro tom de azul claro, né? Mas que se destaque, né? Um do outro, né? Do Minas. Ou se você pensar em cores diferentes da que eu pensei, pode sugerir também. Mas que a gente tenha essas delimitações de região, de estados, e a gente possa fazer essas separações principais aí das cores por atividade e oficinas.
acho q lat long da oficina boom best, okamoto
qua., 19 de ago. às 17:09
encontre esse local 
Atividades Oficinas Spot 26 08 26 01 09 26
Vou importar o arquivo pra... vou mandar o arquivo que eu baixei hoje de planejado sem aceite, aceite e rejeição, pra você analisar.
Atividades Oficinas Spot 23 08 26 25 08 26
Atividades Oficinas Spot 25 08 26
Atividades Oficinas Spot 25 08 26 27 08 26
Atividades Oficinas Spot 24 08 26
Atividades Oficinas Spot 23 08 26
Atividades Oficinas Spot 24 08 26 26 08 26
Atividades Oficinas Spot 22 08 26
app_v2_6_5_fila_revisao_md(1).py
Python
por mim pode fazer o codigo o ultimo que me enviou foi esse 
quarta-feira 10:03
e
Atividades Oficinas Spot 26 08 26
blzz?
Manual_Smart_Estoque_v1.docx - Google Docs.pdf
PDF
alguem quis copiar minha ideia de painel la na empresa e criaram esse anão ai pra controlar estoque de oficinas, mas confesso q n chega nem aos pés da nossa ferramenta, mas o que quero fazer é ver o que podemos fazer para integrar essa idea ao nosso painel, se é possivel

Kkkkk, agora entendi o “anão”. 😂 Mas tem coisa boa aqui para roubar — no bom sentido: incorporar a lógica ao nosso ecossistema, não necessariamente copiar o aplicativo.

Pelo manual, o Smart Estoque faz quatro coisas principais: movimentação de inventário — contar, devolver e solicitar ; controle individual dos módulos/imobilizados sob responsabilidade da oficina ; catálogo visual dos equipamentos ; e acompanhamento do saldo atual, sinalizando normal ou abaixo do ideal para abastecimento.

A parte mais interessante é a própria “regra de ouro” deles: entrada vem das notas fiscais e saída depende dos apontamentos do OFS. E é justamente aí que nosso painel pode ir bem além.

Eu imaginaria um novo módulo chamado 📦 Gestão de Estoque dentro do nosso painel. Só que não seria simplesmente “Oficina X tem 10 câmeras”. Nós já conhecemos oficina, consultor, atividades executadas, tipo de serviço, região e histórico de demanda. Então conseguimos transformar estoque em decisão operacional.

Por exemplo: a oficina tem 3 câmeras. Isoladamente isso não diz quase nada. Mas nosso painel poderia dizer: 3 em estoque | consumo médio 8/mês | 5 serviços previstos nos próximos 7 dias | cobertura estimada 11 dias | 🔴 risco de ruptura. Aí estoque deixa de ser inventário e vira previsão de abastecimento.

E tem uma integração ainda mais poderosa com o que já construímos. Uma manutenção concluída no OFS pode gerar uma saída teórica esperada de determinado equipamento. Se o estoque/inventário diz outra coisa, criamos uma divergência: “OFS indica consumo de 2 módulos, estoque registrou saída de 1”. Isso vira controle de qualidade do próprio apontamento.

Eu arquitetaria o módulo em quatro camadas: Estoque Atual, com saldo por oficina/item; Movimentações, com entrada, consumo, devolução e ajuste; Cobertura, calculando dias/semanas de estoque conforme consumo real; e Previsão, usando agenda e histórico para antecipar necessidade.

E no painel do consultor ficaria muito interessante: além de MCI, MD, OS Perdidas etc., ele teria algo como “3 oficinas em risco de ruptura / 12 itens críticos / 2 divergências OFS × estoque”. Clicou, chega nas oficinas e peças responsáveis.

Tem uma questão que determina o caminho técnico, porém: onde os dados desse Smart Estoque ficam armazenados? Pelo manual, ele roda em AppSheet. Se a fonte for Google Sheets, Excel/Drive, banco SQL ou alguma base acessível, podemos potencialmente consumir esses dados e evitar que a oficina tenha que alimentar dois sistemas.

Então eu não começaria escrevendo código ainda. Primeiro descobriria qual é a fonte de dados do Smart Estoque e quais colunas existem. Se você conseguir me mandar uma exportação da base — CSV/XLSX já serve — eu consigo desenhar para você como encaixar isso no nosso painel e quais métricas novas conseguimos criar.

E tem uma ideia que eu gostei bastante aqui: no nosso Mapa Operacional, no futuro poderíamos clicar numa oficina e enxergar simultaneamente demanda, raio de atendimento e saúde do estoque. Aí aquele mapa começa a responder não só onde precisamos de prestador, mas também onde precisamos colocar equipamento. Isso fica bem maior que um controle de estoque.

Equipamentos Trocados 2026 08 28

Sim. Eu começaria bem mais enxuto do que o projeto completo que desenhamos. A prioridade agora é criar a fundação do estoque sem colocar em risco o que já funciona.

Revendo nosso histórico, o código mais recente que consigo recuperar com segurança do painel de gestão é o v2.6.7, já com Dashboard Executivo, Consultor, Improdutividade, Follow, Dados/Importações, Bases salvas e Cadastro de Oficinas. Ele já conversa com o Supabase e usa as tabelas operacionais que construímos. O que eu não consegui recuperar com a mesma segurança é a última versão do portal específico da oficina/Yeshua. Então eu não mexeria nele no escuro; quando chegarmos nessa etapa, quero trabalhar sobre o arquivo atual dele.

Eu dividiria essa implantação em 3 fases

Fase 1 — criar o motor de estoque e a visão gerencial. É onde eu começaria agora.

No Supabase, criamos um conjunto separado de tabelas. Nada de misturar estoque dentro de atividades_resultado ou atividades_planejadas.

Conceitualmente:

estoque_catalogo_itens
Cadastro único do equipamento: código OFS, descrição, família, ativo/inativo e, futuramente, foto/referências.

estoque_consumos_ofs
Cada utilização que vier desse CSV de equipamentos trocados: OS, data, oficina, técnico, código do item, quantidade, motivo etc.

estoque_inventarios
Cabeçalho de cada contagem: oficina, data, responsável, status.

estoque_inventario_itens
Item + quantidade física encontrada naquela contagem.

estoque_movimentacoes
Essa eu já criaria preparada, mesmo que inicialmente tenha pouca utilização. Futuramente recebe NF, Qflow, devolução, ajuste etc.

E provavelmente uma tabela de importações de estoque, seguindo a filosofia que já usamos no painel, para sabermos qual arquivo entrou, quando entrou e evitar duplicidade.

1. Nosso primeiro importador será o OFS

Na aba Dados / Importações do seu painel de gestão surgiria uma nova opção:

📦 Equipamentos trocados — OFS

Você joga aquele CSV que me mandou.

O sistema identifica os códigos dos itens e grava somente movimentações ainda não existentes.

A partir daí começamos a formar um histórico permanente de:

Oficina → OS → equipamento → quantidade → data → técnico → motivo.

Esse histórico é a matéria-prima para nossa Curva ABC.

Não precisamos decidir manualmente quais itens são A, B ou C. O sistema calcula pelo consumo acumulado.

2. A primeira versão da aba Estoque do seu painel

Eu não começaria tentando mostrar saldo, porque ainda não temos inventário-base.

A primeira versão poderia mostrar:

Consumo de equipamentos

Total consumido
Itens diferentes
Oficinas consumidoras
Consumo por oficina
Top equipamentos
Curva ABC
Motivos de envio/troca
Evolução semanal/mensal

E aí já conseguimos responder:

“Quais são os equipamentos que mais pressionam nossa operação?”

Essa parte já funciona só com o arquivo OFS que temos hoje.

3. Depois fazemos o marco zero da Yeshua

Essa seria a Fase 2.

Você pede para a Yeshua fazer aquela última contagem no Drive.

Ela termina.

Você baixa a planilha e me manda.

Nós fazemos um importador temporário para transformar aquela planilha em:

Inventário Inicial Yeshua — data X

A partir daquele instante o Drive morreu para ela. 😂

E é aqui que eu vou precisar que você me mande o app.py atual do painel da Yeshua/oficina, porque não quero reconstruir esse portal baseado numa versão antiga.

Dentro do painel que ela já utiliza acrescentamos:

📦 Estoque

E inicialmente apenas três coisas:

Meu Estoque | Fazer Inventário | Histórico de Contagens

Nada de catálogo sofisticado ainda.

A oficina abre Fazer Inventário, vê os itens e digita a quantidade física. Salva. Supabase registra a fotografia.

A partir desse momento nasce o saldo projetado

Suponha que no marco zero:

VBOX PRO = 15

Depois o OFS mostra:

OS 001 → utilizou 1
OS 002 → utilizou 2
OS 003 → utilizou 1

Nosso painel passa a calcular:

Inventário-base 15 − consumo OFS 4 = saldo projetado 11.

Se na próxima contagem física a Yeshua disser que encontrou 10:

Projetado: 11 | Físico: 10 | Divergência: -1

E guardamos a divergência. Não apagamos a história.

E aí vem a parte que você quer no painel gerencial

Depois de validarmos isso com a Yeshua, começamos a cruzar:

Curva ABC + saldo projetado + agenda futura.

Aí sua aba Estoque deixa de ser apenas estoque e passa a apresentar Risco Operacional.

Exemplo:

YESHUA

🔴 VMOV+ 64 GB — Curva A
Estoque projetado: 2
Demanda/necessidade prevista: 5
Cobertura: insuficiente
Risco: 3 atendimentos

Isso conversa diretamente com a gestão que já fazemos.

Se na segunda-feira existem serviços planejados para sexta e a oficina não tem equipamento suficiente, você não precisa esperar sexta chegar para descobrir isso pela improdutividade.

E Qflow/NF entram somente na Fase 3

Quando você conseguir esses arquivos, nós olhamos o formato.

A arquitetura já estará preparada.

Pode acabar acontecendo algo como:

Qflow = solicitado
NF = expedido
Oficina = recebido/contado
OFS = consumido

Isso permitiria inclusive algo muito interessante:

Pedido: 10
Expedido: 10
Recebido: 10
Consumido: 6
Saldo teórico: 4
Contagem física: 3
Divergência: -1

Mas eu não esperaria essa fase para começar.

E o catálogo?

Eu colocaria como Fase 4.

O cadastro de itens já nasce agora, justamente para não precisarmos alterar banco depois. Inicialmente ele contém apenas código OFS + nomenclatura + família/classificação quando disponível.

Depois enriquecemos com foto, referência logística, memória, operadora, acessórios, compatibilidades e vista explodida.

Assim não tentamos construir uma concessionária Mercedes-Benz na primeira terça-feira do projeto. 😂

Portanto, minha sequência prática seria esta:

Não mexer ainda no painel da oficina.
Pegar o v2.6.7 do painel gerencial como base.
Criar as novas tabelas de estoque no Supabase.
Criar o importador Equipamentos Trocados — OFS.
Criar a primeira aba 📦 Estoque com consumo + Curva ABC.
Validar se histórico/importação estão funcionando corretamente.
Você me manda a última planilha inventariada da Yeshua.
Você me manda o app.py atual do portal da Yeshua/oficina.
Implantamos inventário diretamente no portal.
Depois cruzamos estoque × planejamento futuro e criamos os alertas preventivos.
Qflow/NF e catálogo entram posteriormente.

Eu acho esse começo bem controlado: primeiro construímos a inteligência com dados que já possuímos; depois substituímos o Drive; e só então automatizamos a cadeia logística inteira.

hoje 14:48
Texto colado(7).txt
Documento
appy da yeshua atual

Perfeito. Agora tenho o app atual da Yeshua e já conferi a estrutura. Esse arquivo passa a ser nossa referência para o portal da oficina.

E ele está numa posição muito boa para receber estoque. O portal já está conectado ao mesmo Supabase do painel principal e inclusive possui busca paginada para bases maiores que 1.000 registros.

Hoje ele já faz bastante coisa: desempenho operacional, detalhamento das OS, planejamento futuro e Follow integrado. O Follow inclusive grava respostas da própria oficina no Supabase, então o portal já deixou de ser puramente consulta na prática — já existe a infraestrutura de escrita que precisaremos para o inventário.

Também confirmei que essa é a versão Portal da Oficina MVP 1.4.0.

Portanto, eu mudaria ligeiramente nosso plano: não precisamos criar nenhuma aplicação nova para a Yeshua.

A evolução natural será o próprio portal virar algo como:

Portal YESHUA
Visão Geral | Planejamento | OS | Follow | 📦 Estoque

Dentro de 📦 Estoque, na primeira versão, eu colocaria somente Meu Estoque, Fazer Inventário e Histórico de Contagens. Depois entram Solicitações/Devoluções e, posteriormente, Catálogo.

E encontrei uma conexão que vai ficar excelente: o Follow atual já pergunta sobre “Falta de equipamento ou ferramenta” e “Falta de peça ou insumo” como motivos de impedimento. Quando tivermos estoque, no futuro o próprio sistema poderá verificar isso antes de perguntar à oficina.

Tem apenas uma coisa que eu não mexeria junto com estoque agora: esse portal ainda contém internamente a nomenclatura antiga “No-show”, inclusive nos cálculos e telas. O painel de gestão já evoluiu para OS Perdidas, mas este código da Yeshua ainda não recebeu essa atualização. Isso merece uma correção posteriormente, mas eu separaria da implantação do estoque para não misturar dois assuntos e gerar regressão.

Próximo passo técnico agora: montar o SQL da fundação do estoque no Supabase primeiro. Depois disso fazemos a primeira alteração no painel de gestão para importar Equipamentos Trocados — OFS. Só depois tocamos neste app.py da Yeshua para adicionar Inventário.

Ou seja: agora temos as duas pontas identificadas. Gestão alimenta/monitora → Supabase centraliza → Yeshua conta e consulta. Dá para começar a construir com segurança.

ok vamos

Perfeito. Vamos começar pela Etapa 1: fundação do estoque no Supabase, sem alterar ainda nem o painel de gestão nem o portal da Yeshua.

Vou preparar o primeiro SQL já pensando em: catálogo mestre de itens, consumo OFS, inventários físicos, itens contados, movimentações futuras e controle das importações. Ele também já ficará preparado para NF/Qflow depois, sem precisarmos redesenhar o banco.

A ordem será SQL → executar no Supabase → validar → depois alterar o app. Assim não colocamos a versão atual em risco.

app_v2_6_7_correcao_detalhe_os_perdidas(1).py
Python
voltando um pouco o assunto 

Oscar, pelo que você mostrou, eu não concluiria que você “fez algo ilícito”. Há uma diferença grande entre crime/ilegalidade, eventual descumprimento de uma política interna e uso de uma ferramenta fora da modalidade aprovada.

O print que você encontrou indica que ChatGPT é uma ferramenta admitida pela empresa, mas sob a condição “Only Teams, limited access”. Como você me diz agora que esta conta é pessoal, o que temos é um possível desalinhamento com a política corporativa de uso da ferramenta. Isso, por si só, não permite concluir que houve uma infração legal ou uma violação do GDPR.

Também importa o conteúdo que utilizamos. Pelo que construímos, boa parte são dados operacionais B2B: oficinas, OS, equipamentos, estoque, clientes empresariais, placas de veículos de frota, indicadores etc. Entretanto, em algumas bases apareceram nomes de técnicos, contatos/WhatsApp e possivelmente outros dados pessoais. Então eu não diria que “não houve nenhum dado pessoal”; houve potencialmente alguns, e é justamente aí que devemos ser cuidadosos.

O caminho mais prudente agora é bem administrável:

Não envie novos arquivos corporativos com dados pessoais nesta conta pessoal, até esclarecer com a empresa qual ambiente do ChatGPT deve ser usado.
Não precisamos jogar fora o projeto. Código, regras de negócio, estrutura SQL, cálculos de MCI/MD, estoque etc. podem continuar existindo e depois ser levados ao ambiente autorizado.
Nas bases futuras, podemos criar uma camada de sanitização antes da importação, removendo CPF, telefone e outros campos pessoais desnecessários. No estoque já decidimos retirar também o técnico.
Para colocar a solução oficialmente em produção, aí sim vale envolver TI/Segurança para decidir onde Streamlit/Supabase ficarão hospedados ou migrar para a infraestrutura corporativa.

E há um detalhe positivo no que você descobriu: agora sabemos exatamente qual pergunta fazer à liderança/Segurança, em vez de tentar interpretar GDPR sozinhos:

“Estou desenvolvendo um protótipo operacional utilizando dados predominantemente B2B. Identifiquei que a política lista ChatGPT como ‘Only Teams, limited access’. Qual ambiente corporativo autorizado devo utilizar para continuar o desenvolvimento e qual é a orientação para os dados já utilizados no protótipo?”

Essa abordagem é transparente e objetiva. Eu não apagaria banco, código ou sairia desfazendo o projeto precipitadamente. Primeiro confirmaria a orientação interna e, enquanto isso, daqui para frente trabalharia com bases sanitizadas.

Se você quiser, inclusive, antes de continuarmos o estoque eu posso adaptar nosso importador para remover automaticamente campos pessoais, para não depender de você lembrar de limpar cada CSV.

vamos fazer isso 

Fechado. Já fiz essa proteção sem mexer nas regras operacionais do painel.

Criei a v2.7.1, mantendo tudo da v2.7.0 e acrescentando uma camada de minimização na persistência. Agora, quando Planejado, Resultado ou Pré-agenda forem gravados, o conteúdo bruto em JSON descarta automaticamente campos como CPF, RG, CNH, telefone, celular, WhatsApp e e-mail. Mantive Recurso, porque o painel ainda usa isso na Pré-agenda e na revisão MD. No estoque, técnico continua totalmente fora da persistência.

Baixar app_v2_7_1_sanitizacao_dados.py

Também fiz uma pequena migração para remover definitivamente a coluna tecnico que ficou criada na tabela estoque_consumos_ofs. Assim ela nem fica disponível para alguém preencher no futuro por engano:

Baixar sql_v2_7_1_remover_tecnico_estoque.sql

A ordem agora é simples: primeiro rode esse SQL pequeno no Supabase. Depois substitua o app.py pela v2.7.1. Eu também compilei o arquivo e a sintaxe está OK.

Importante: isso protege as novas importações. Não apaga automaticamente eventuais CPF/telefone que já estejam armazenados dentro dos JSONs históricos. Depois que validarmos essa versão, podemos fazer uma segunda etapa controlada para varrer o que já existe no Supabase e eliminar esses campos antigos sem tocar em OS, placa, oficina, MCI, MD ou histórico operacional.

app_v2_7_1_sanitizacao_dados.py
Código
sql_v2_7_1_remover_tecnico_estoque.sql
Código
blz

Beleza. 👍 E esse print confirma que fizemos a escolha certa ao sanitizar as bases.

A orientação do treinamento é bem explícita: “Inserir código-fonte, PII de clientes ou dados financeiros em qualquer ferramenta de IA” aparece entre as proibições. Também orienta tratar tudo colocado em um prompt de IA como se estivesse sendo divulgado a terceiros.

Então, daqui para frente, vamos manter uma regra conservadora no nosso projeto: CPF, telefone/celular, e-mail e qualquer outro PII desnecessário ficam fora das bases que utilizarmos aqui. Para estoque, nem nome de técnico precisamos.

Isso nos permite continuar trabalhando na lógica do protótipo com uma exposição muito menor, enquanto você verifica internamente o ambiente corporativo adequado para uma eventual implantação oficial.

Agora podemos voltar para onde paramos: SQL pequeno de remoção do técnico → v2.7.1 → primeira importação do OFS de equipamentos.

eu poderei solici

import streamlit as st
import pandas as pd
import plotly.express as px
from datetime import date, datetime, timedelta
from supabase import create_client

st.set_page_config(
page_title="Portal da Oficina | YESHUA RASTREAMENTO",
page_icon="🏢",
layout="wide",
)

OFICINA_PORTAL = "YESHUA RASTREAMENTO"
CACHE_TTL_SEGUNDOS = 60

def obter_cliente_supabase():
try:
url = st.secrets["SUPABASE_URL"]

    # Usa o mesmo padrão do painel principal.
    if "SUPABASE_SERVICE_KEY" in st.secrets:
        key = st.secrets["SUPABASE_SERVICE_KEY"]
    elif "SUPABASE_KEY" in st.secrets:
        key = st.secrets["SUPABASE_KEY"]
    else:
        raise KeyError("Chave do Supabase não encontrada.")

except Exception:
    st.error(
        "O portal ainda não está conectado ao Supabase. "
        "Configure SUPABASE_URL e SUPABASE_SERVICE_KEY "
        "(ou SUPABASE_KEY) nos Secrets do Streamlit."
    )
    st.stop()

return create_client(url, key)

cliente = obter_cliente_supabase()

Compatibilidade com o motor reaproveitado do painel principal.
Algumas funções internas usam os nomes SUPABASE / ERRO_SUPABASE.

SUPABASE = cliente
ERRO_SUPABASE = None

def buscar_todos(tabela: str, ordem: str | None = None, desc: bool = False):
consulta = cliente.table(tabela).select("*")
if ordem:
consulta = consulta.order(ordem, desc=desc)
resposta = consulta.execute()
return resposta.data or []




import html

import io

import re

import unicodedata

import uuid

from datetime import date, datetime

from typing import Any

from urllib.parse import quote, urlsplit, urlunsplit

from zoneinfo import ZoneInfo

import pandas as pd

import plotly.express as px

import streamlit as st

from supabase import Client, create_client

TABELA_POR_TIPO = {
"planejado": "atividades_planejadas",
"resultado": "atividades_resultado",
}

DATA_CORTE_NOVA_REGRA = "2026-08-08"

CACHE_TTL_SEGUNDOS = 60

def buscar_todos(
tabela: str,
colunas: str = "*",
filtros: dict[str, Any] | None = None,
ordem: str | None = None,
desc: bool = False,
tamanho_pagina: int = 1000,
) -> list[dict]:
"""Busca todos os registros, inclusive quando houver mais de 1.000 linhas."""
cliente = exigir_supabase()
registros: list[dict] = []
inicio = 0

while True:
    consulta = cliente.table(tabela).select(colunas)

    for coluna, valor in (filtros or {}).items():
        consulta = consulta.eq(coluna, valor)

    if ordem:
        consulta = consulta.order(ordem, desc=desc)

    resposta = consulta.range(
        inicio,
        inicio + tamanho_pagina - 1,
    ).execute()

    lote = resposta.data or []
    registros.extend(lote)

    if len(lote) < tamanho_pagina:
        break

    inicio += tamanho_pagina

return registros

def carregar_base(
tipo: str,
data_operacional: str,
) -> pd.DataFrame:
tabela = TABELA_POR_TIPO[tipo]
registros = buscar_todos(
tabela,
filtros={"data_operacional": data_operacional},
ordem="id",
)

linhas = []

for registro in registros:
    dados = dict(registro.get("dados") or {})

    padrao = {
        "Ticket Jira": registro.get("ticket_jira", ""),
        "OS": registro.get("os", ""),
        "Placa": registro.get("placa", ""),
        "Oficina": registro.get("oficina", ""),
        "Cliente": registro.get("cliente", ""),
        "Estado": registro.get("estado", ""),
        "Cidade": registro.get("cidade", ""),
        "Tipo de Atividade": registro.get("tipo_atividade", ""),
        "Status da Atividade": registro.get(
            "status_atividade",
            "",
        ),
        "Recurso": registro.get("recurso", ""),
        "__Data Operacional": registro.get(
            "data_operacional",
            data_operacional,
        ),
    }

    if tipo == "planejado":
        padrao.update(
            {
                "__Primeira Aparição": registro.get(
                    "primeira_aparicao",
                    "",
                ),
                "__Primeira Aparição Data": registro.get(
                    "primeira_aparicao_data",
                    "",
                ),
                "__Última Aparição": registro.get(
                    "ultima_aparicao",
                    "",
                ),
                "__Ativa no Planejamento": bool(
                    registro.get(
                        "ativa_no_planejamento",
                        True,
                    )
                ),
                "__Arquivo Primeira Aparição": registro.get(
                    "nome_arquivo_primeira_aparicao",
                    "",
                ),
            }
        )

    dados.update(padrao)
    linhas.append(dados)

return pd.DataFrame(linhas)

def carregar_consolidado(datas: list[str]) -> pd.DataFrame:
"""Concilia todas as datas completas e cria a visão histórica geral."""
partes = []

for data_operacional in datas:
    planejado = carregar_base("planejado", data_operacional)
    resultado = carregar_base("resultado", data_operacional)

    if planejado.empty and resultado.empty:
        continue

    conciliacao_data = conciliar_bases(
        planejado,
        resultado,
    )
    conciliacao_data.insert(
        0,
        "Data Operacional",
        data_operacional,
    )
    partes.append(conciliacao_data)

if not partes:
    return pd.DataFrame()

return pd.concat(
    partes,
    ignore_index=True,
    sort=False,
)

def carregar_consolidado_portal_todos_servicos(
datas: list[str],
) -> pd.DataFrame:
"""Concilia todas as atividades da oficina, não apenas manutenções."""
partes = []

for data_operacional in datas:
    planejado = carregar_base(
        "planejado",
        data_operacional,
    )
    resultado = carregar_base(
        "resultado",
        data_operacional,
    )

    if planejado.empty and resultado.empty:
        continue

    conciliacao_data = (
        conciliar_bases_portal_todos_servicos(
            planejado,
            resultado,
        )
    )

    conciliacao_data.insert(
        0,
        "Data Operacional",
        data_operacional,
    )

    partes.append(
        conciliacao_data
    )

if not partes:
    return pd.DataFrame()

return pd.concat(
    partes,
    ignore_index=True,
    sort=False,
)




def carregar_oficinas() -> pd.DataFrame:
registros = buscar_todos(
"oficinas",
ordem="nome_oficina",
)

if not registros:
    return pd.DataFrame()

return pd.DataFrame(
    {
        "ID": [r.get("codigo_oficina", "") for r in registros],
        "Oficina": [r.get("nome_oficina", "") for r in registros],
        "Cidade-base": [r.get("cidade", "") for r in registros],
        "UF-base": [r.get("uf", "") for r in registros],
        "Consultor": [r.get("consultor", "Não definido") for r in registros],
        "WhatsApp": [r.get("whatsapp", "") for r in registros],
        "Prioridade": [r.get("prioridade", "Normal") for r in registros],
        "Ativa": ["Sim" if r.get("ativa", True) else "Não" for r in registros],
        "Observações": [r.get("observacoes", "") for r in registros],
        "Chave Oficina": [r.get("chave_oficina", "") for r in registros],
    }
)

def conciliar_bases(
planejado: pd.DataFrame,
resultado: pd.DataFrame,
) -> pd.DataFrame:
"""
Regra híbrida:
- datas anteriores a DATA_CORTE_NOVA_REGRA usam a lógica histórica;
- a partir da data de corte, usa primeira aparição da OS para
classificar Agendada x Extra/Encaixe.
"""
planejado = filtrar_somente_manutencoes(planejado)
resultado = filtrar_somente_manutencoes(resultado)

planejado = criar_chaves(planejado)
resultado = criar_chaves(resultado)

if "Status da Atividade" not in resultado.columns:
    resultado["Status da Atividade"] = ""

if "Status da Atividade" not in planejado.columns:
    planejado["Status da Atividade"] = ""

# Compatibilidade com todo o histórico já salvo:
# se uma importação antiga não tiver estes campos, o painel continua
# funcionando e exibe o detalhe como vazio, sem alterar os indicadores.
if "Razão da Improdutiva" not in resultado.columns:
    resultado["Razão da Improdutiva"] = ""

if "Observação do Técnico (Improdutiva)" not in resultado.columns:
    resultado["Observação do Técnico (Improdutiva)"] = ""

if "__Ativa no Planejamento" not in planejado.columns:
    planejado["__Ativa no Planejamento"] = True

if "__Primeira Aparição Data" not in planejado.columns:
    planejado["__Primeira Aparição Data"] = ""

if "__Data Operacional" not in planejado.columns:
    planejado["__Data Operacional"] = ""

resumo_resultado = (
    resultado
    .groupby("Chave Atendimento", dropna=False)
    .agg(
        Ticket_resultado=("Ticket Jira", "first"),
        Placa_resultado=("Placa", "first"),
        OS_resultado=("OS", juntar_unicos),
        Oficina_resultado=("Oficina", "first"),
        Status_resultado=("Status da Atividade", juntar_unicos),
        Razao_improdutiva=(
            "Razão da Improdutiva",
            juntar_unicos,
        ),
        Observacao_tecnico_improdutiva=(
            "Observação do Técnico (Improdutiva)",
            juntar_unicos,
        ),
        Qtd_resultado=("Chave Atendimento", "size"),
    )
    .reset_index()
)

resumo_planejado = (
    planejado
    .groupby("Chave Atendimento", dropna=False)
    .agg(
        Ticket_planejado=("Ticket Jira", "first"),
        Placa_planejada=("Placa", "first"),
        OS_planejada=("OS", juntar_unicos),
        Oficina_planejada=("Oficina", "first"),
        Status_planejado=("Status da Atividade", juntar_unicos),
        Primeira_aparicao_data=(
            "__Primeira Aparição Data",
            "first",
        ),
        Data_operacional_planejada=(
            "__Data Operacional",
            "first",
        ),
        Ativa_planejamento=(
            "__Ativa no Planejamento",
            "max",
        ),
        Qtd_planejada=("Chave Atendimento", "size"),
    )
    .reset_index()
)

conciliacao = resumo_planejado.merge(
    resumo_resultado,
    on="Chave Atendimento",
    how="outer",
    indicator=True,
)

# Auditoria de substituição de OS:
# se a OS planejada não aparece pelo mesmo atendimento, procuramos
# outra OS no resultado com o MESMO Ticket Jira + MESMA placa.
# Nessa situação, não declaramos no-show automaticamente.
def chave_ticket_placa(ticket, placa) -> str:
    ticket_norm = normalizar_texto(ticket)
    placa_norm = normalizar_texto(placa)

    if not ticket_norm or not placa_norm:
        return ""

    return f"{ticket_norm}||{placa_norm}"

chaves_resultado_ticket_placa = set()

for _, item in resumo_resultado.iterrows():
    chave = chave_ticket_placa(
        item.get("Ticket_resultado", ""),
        item.get("Placa_resultado", ""),
    )
    if chave:
        chaves_resultado_ticket_placa.add(chave)

def encontrou_substituicao_ticket_placa(linha) -> bool:
    if linha.get("_merge") != "left_only":
        return False

    chave = chave_ticket_placa(
        linha.get("Ticket_planejado", ""),
        linha.get("Placa_planejada", ""),
    )

    return bool(
        chave
        and chave in chaves_resultado_ticket_placa
    )

conciliacao["Possível substituição de OS"] = conciliacao.apply(
    encontrou_substituicao_ticket_placa,
    axis=1,
)

def data_referencia_linha(linha) -> str | None:
    data_operacional = converter_data_operacional(
        linha.get("Data_operacional_planejada", "")
    )
    return data_operacional

def usar_regra_nova(linha) -> bool:
    data_operacional = data_referencia_linha(linha)

    if not data_operacional:
        return False

    return data_operacional >= DATA_CORTE_NOVA_REGRA

def eh_agendada_nova(linha) -> bool:
    if linha.get("_merge") == "right_only":
        return False

    if not bool(linha.get("Ativa_planejamento", False)):
        return False

    primeira = converter_data_operacional(
        linha.get("Primeira_aparicao_data", "")
    )
    data_operacional = data_referencia_linha(linha)

    if not primeira or not data_operacional:
        return False

    return primeira < data_operacional

def eh_agendada_historica(linha) -> bool:
    # Na regra antiga, toda manutenção válida presente no planejado
    # era considerada agendada, independentemente de primeira aparição.
    if linha.get("_merge") == "right_only":
        return False

    status_planejado = linha.get("Status_planejado", "")

    if status_cancelado(status_planejado):
        return False

    return True

def eh_agendada(linha) -> bool:
    if usar_regra_nova(linha):
        return eh_agendada_nova(linha)

    return eh_agendada_historica(linha)

conciliacao["Origem Agendamento"] = conciliacao.apply(
    lambda linha: (
        "Agendada"
        if eh_agendada(linha)
        else "Extra / encaixe"
    ),
    axis=1,
)

def classificar(linha) -> str:
    origem_merge = linha["_merge"]
    status_resultado = linha.get("Status_resultado", "")
    status_planejado = linha.get("Status_planejado", "")
    agendada = linha["Origem Agendamento"] == "Agendada"
    ativa = bool(linha.get("Ativa_planejamento", False))
    nova_regra = usar_regra_nova(linha)

    # Histórico antigo preserva a lógica anterior.
    if not nova_regra:
        if origem_merge == "left_only":
            if status_cancelado(status_planejado):
                return "Cancelada no agendamento"
            if bool(
                linha.get(
                    "Possível substituição de OS",
                    False,
                )
            ):
                return "Possível substituição de OS"
            return "No-show"

        if origem_merge == "right_only":
            if status_improdutivo(status_resultado):
                return "Improdutiva extra"
            if status_cancelado(status_resultado):
                return "Cancelada extra"
            if status_executado(status_resultado):
                return "Executada extra"
            return "Evento extra"

        if status_cancelado(status_planejado):
            return "Cancelada no agendamento"

        if status_improdutivo(status_resultado):
            return "Improdutiva agendada"

        if status_cancelado(status_resultado):
            return "Cancelada"

        if status_executado(status_resultado):
            return "Executada agendada"

        return "Status intermediário agendado"

    # Nova regra, a partir da data de corte.
    if origem_merge == "left_only" and not ativa:
        return "Retirada do agendamento"

    if (
        origem_merge in {"left_only", "both"}
        and ativa
        and status_cancelado(status_planejado)
    ):
        return "Cancelada no agendamento"

    if origem_merge == "left_only":
        if agendada:
            if bool(
                linha.get(
                    "Possível substituição de OS",
                    False,
                )
            ):
                return "Possível substituição de OS"
            return "No-show"
        return "Encaixe não realizado"

    if origem_merge == "right_only":
        if status_improdutivo(status_resultado):
            return "Improdutiva extra"
        if status_cancelado(status_resultado):
            return "Cancelada extra"
        if status_executado(status_resultado):
            return "Executada extra"
        return "Evento extra"

    if status_improdutivo(status_resultado):
        return (
            "Improdutiva agendada"
            if agendada
            else "Improdutiva extra"
        )

    if status_cancelado(status_resultado):
        return (
            "Cancelada"
            if agendada
            else "Cancelada extra"
        )

    if status_executado(status_resultado):
        return (
            "Executada agendada"
            if agendada
            else "Executada extra"
        )

    return (
        "Status intermediário agendado"
        if agendada
        else "Status intermediário extra"
    )

conciliacao["Classificação"] = conciliacao.apply(
    classificar,
    axis=1,
)

def explicar_classificacao(linha) -> str:
    classificacao = linha.get("Classificação", "")
    status_planejado = texto_limpo(
        linha.get("Status_planejado", "")
    )
    status_resultado = texto_limpo(
        linha.get("Status_resultado", "")
    )
    primeira = texto_limpo(
        linha.get("Primeira_aparicao_data", "")
    )
    data_operacional = texto_limpo(
        linha.get("Data_operacional_planejada", "")
    )
    nova_regra = usar_regra_nova(linha)

    if not nova_regra:
        return (
            "Histórico anterior à data de corte: classificação "
            "preservada pela lógica antiga do painel."
        )

    if classificacao == "Executada agendada":
        return (
            f"Manutenção já estava agendada antes de {data_operacional} "
            f"(primeira aparição: {primeira}) e foi executada."
        )
    if classificacao == "Executada extra":
        return (
            "Manutenção não tinha prova de agendamento para essa data "
            "antes do início do dia e foi executada como extra/encaixe."
        )
    if classificacao == "Improdutiva agendada":
        return (
            "Manutenção já estava agendada antes do dia e terminou "
            f"improdutiva/não concluída: {status_resultado}"
        )
    if classificacao == "Improdutiva extra":
        return (
            "Manutenção extra/encaixe terminou improdutiva/não concluída: "
            f"{status_resultado}"
        )
    if classificacao == "Cancelada":
        return (
            "Manutenção estava agendada válida e apareceu cancelada "
            f"posteriormente no resultado: {status_resultado}"
        )
    if classificacao == "Cancelada extra":
        return (
            "Cancelamento de manutenção sem prova de agendamento "
            "anterior para essa mesma data."
        )
    if classificacao == "Cancelada no agendamento":
        return (
            "A manutenção já estava cancelada na fotografia vigente "
            f"do agendamento: {status_planejado}"
        )
    if classificacao == "Possível substituição de OS":
        return (
            "A OS agendada não apareceu pelo mesmo atendimento, mas "
            "foi localizada outra OS no resultado com o mesmo Ticket "
            "Jira + mesma placa. O caso foi retirado do no-show para "
            "auditoria de possível troca/substituição de OS."
        )
    if classificacao == "No-show":
        return (
            "Manutenção estava agendada antes do dia, a própria OS não "
            "apareceu no resultado e nenhuma outra OS com o mesmo Ticket "
            "Jira + mesma placa foi localizada. Classificada como "
            "no-show provável."
        )
    if classificacao == "Encaixe não realizado":
        return (
            "A manutenção surgiu no próprio dia como extra/encaixe "
            "e não apareceu no resultado; não conta como no-show."
        )
    if classificacao == "Retirada do agendamento":
        return (
            "A OS apareceu em fotografia anterior, mas não está mais "
            "no agendamento vigente dessa data."
        )

    return (
        "Status não reconhecido pelas regras principais. "
        f"Planejado: {status_planejado}; resultado: {status_resultado}."
    )

conciliacao["Motivo da Classificação"] = conciliacao.apply(
    explicar_classificacao,
    axis=1,
)

conciliacao["Regra Aplicada"] = conciliacao.apply(
    lambda linha: (
        "Nova regra"
        if usar_regra_nova(linha)
        else "Regra histórica"
    ),
    axis=1,
)

conciliacao["Troca de OS"] = conciliacao.apply(
    lambda linha: (
        "Sim"
        if (
            linha["_merge"] == "both"
            and texto_limpo(
                linha.get("OS_planejada", "")
            )
            != texto_limpo(
                linha.get("OS_resultado", "")
            )
        )
        else "Não"
    ),
    axis=1,
)

conciliacao["Oficina"] = conciliacao[
    "Oficina_planejada"
].fillna(conciliacao["Oficina_resultado"])

conciliacao["Ticket"] = conciliacao[
    "Ticket_planejado"
].fillna(conciliacao["Ticket_resultado"])

conciliacao["Placa"] = conciliacao[
    "Placa_planejada"
].fillna(conciliacao["Placa_resultado"])

return conciliacao

def converter_data_operacional(valor) -> str | None:
texto = texto_limpo(valor)

if not texto:
    return None

for dayfirst in (True, False):
    try:
        data_convertida = pd.to_datetime(
            texto,
            dayfirst=dayfirst,
            errors="raise",
        )
        return data_convertida.date().isoformat()
    except Exception:
        pass

return None

def criar_chaves(df: pd.DataFrame) -> pd.DataFrame:
df = df.copy()

for coluna in ["Ticket Jira", "Placa", "OS"]:
    if coluna not in df.columns:
        df[coluna] = ""

    df[coluna] = df[coluna].apply(texto_limpo)

df["Chave Ticket"] = df["Ticket Jira"].apply(normalizar_texto)
df["Chave Placa"] = df["Placa"].apply(normalizar_texto)
df["Chave OS"] = df["OS"].apply(normalizar_texto)

# Os indicadores do painel são baseados em OS.
# Quando houver OS, ela será a chave principal. A regra anterior
# usava Ticket + Placa e podia agrupar OS diferentes.
possui_os = df["Chave OS"] != ""

df["Chave Atendimento"] = ""

df.loc[possui_os, "Chave Atendimento"] = (
    "OS|"
    + df.loc[possui_os, "Chave OS"]
    + "|"
    + df.loc[possui_os, "Chave Placa"]
)

sem_os = ~possui_os

df.loc[sem_os, "Chave Atendimento"] = (
    "TICKET|"
    + df.loc[sem_os, "Chave Ticket"]
    + "|"
    + df.loc[sem_os, "Chave Placa"]
)

sem_identificador = (
    (df["Chave Ticket"] == "")
    & (df["Chave OS"] == "")
    & (df["Chave Placa"] == "")
)

df.loc[sem_identificador, "Chave Atendimento"] = (
    "LINHA|" + df.index[sem_identificador].astype(str)
)

return df

def enriquecer_com_cadastro(
base: pd.DataFrame,
cadastro: pd.DataFrame,
) -> pd.DataFrame:
resultado = base.copy()

if "Oficina" not in resultado.columns:
    resultado["Oficina"] = ""

resultado["Chave Oficina"] = resultado["Oficina"].apply(
    normalizar_texto
)

colunas = [
    "Chave Oficina",
    "Cidade-base",
    "UF-base",
    "Consultor",
    "WhatsApp",
    "Prioridade",
]

resultado = resultado.merge(
    cadastro[colunas].drop_duplicates(
        subset=["Chave Oficina"]
    ),
    on="Chave Oficina",
    how="left",
)

resultado["Consultor"] = resultado["Consultor"].fillna(
    "Não definido"
)
resultado["Cidade-base"] = resultado["Cidade-base"].fillna("")
resultado["UF-base"] = resultado["UF-base"].fillna("")
resultado["WhatsApp"] = resultado["WhatsApp"].fillna("")
resultado["Prioridade"] = resultado["Prioridade"].fillna("Normal")

return resultado

def exigir_supabase() -> Client:
if SUPABASE is None:
st.error(ERRO_SUPABASE or "Supabase não conectado.")
st.stop()

return SUPABASE

def filtrar_somente_manutencoes(df: pd.DataFrame) -> pd.DataFrame:
"""
Mantém somente atividades cujo Tipo de Atividade contenha
a palavra MANUTEN, cobrindo manutenção, manutenções,
manutenção corretiva, preventiva e demais variações.
"""
if df is None or df.empty:
return pd.DataFrame(columns=df.columns if df is not None else [])

if "Tipo de Atividade" not in df.columns:
    raise ValueError(
        "A base não possui a coluna 'Tipo de Atividade'. "
        "Não foi possível filtrar somente manutenções."
    )

base = df.copy()

mascara = base["Tipo de Atividade"].apply(
    lambda valor: "MANUTEN" in normalizar_texto(valor)
)

return base[mascara].copy().reset_index(drop=True)

def juntar_unicos(valores) -> str:
itens = sorted(
{
texto_limpo(valor)
for valor in valores
if texto_limpo(valor)
}
)
return " | ".join(itens)

def listar_bases() -> pd.DataFrame:
registros = buscar_todos(
"bases_importadas",
ordem="data_operacional",
desc=True,
)
return pd.DataFrame(registros)

def normalizar_texto(valor) -> str:
texto = texto_limpo(valor).upper()
texto = unicodedata.normalize("NFKD", texto)
texto = "".join(
caractere
for caractere in texto
if not unicodedata.combining(caractere)
)
texto = re.sub(r"\s+", " ", texto)
return texto.strip()

def status_cancelado(valor) -> bool:
return "CANCEL" in status_normalizado(valor)

def status_executado(valor) -> bool:
status = status_normalizado(valor)
return any(
termo in status
for termo in [
"CONCLUID",
"EXECUTAD",
"FINALIZAD",
"COMPLET",
"REALIZAD",
]
)

def status_improdutivo(valor) -> bool:
status = status_normalizado(valor)
return any(
termo in status
for termo in [
"NAO CONCLUIDO",
"NAO CONCLUIDA",
"IMPRODUTIVO",
"IMPRODUTIVA",
"SEM SUCESSO",
]
)

def status_normalizado(valor) -> str:
return normalizar_texto(valor)

def texto_limpo(valor) -> str:
if valor is None:
return ""

try:
    if pd.isna(valor):
        return ""
except (TypeError, ValueError):
    pass

texto = str(valor).strip()

if texto.lower() in {"nan", "none", "null", "nat"}:
    return ""

return texto

def conciliar_bases_portal_todos_servicos(
planejado: pd.DataFrame,
resultado: pd.DataFrame,
) -> pd.DataFrame:
"""
Regra híbrida:
- datas anteriores a DATA_CORTE_NOVA_REGRA usam a lógica histórica;
- a partir da data de corte, usa primeira aparição da OS para
classificar Agendada x Extra/Encaixe.
"""
planejado = criar_chaves(planejado)
resultado = criar_chaves(resultado)

if "Status da Atividade" not in resultado.columns:
    resultado["Status da Atividade"] = ""

if "Status da Atividade" not in planejado.columns:
    planejado["Status da Atividade"] = ""

# Compatibilidade com todo o histórico já salvo:
# se uma importação antiga não tiver estes campos, o painel continua
# funcionando e exibe o detalhe como vazio, sem alterar os indicadores.
if "Razão da Improdutiva" not in resultado.columns:
    resultado["Razão da Improdutiva"] = ""

if "Observação do Técnico (Improdutiva)" not in resultado.columns:
    resultado["Observação do Técnico (Improdutiva)"] = ""

if "__Ativa no Planejamento" not in planejado.columns:
    planejado["__Ativa no Planejamento"] = True

if "__Primeira Aparição Data" not in planejado.columns:
    planejado["__Primeira Aparição Data"] = ""

if "__Data Operacional" not in planejado.columns:
    planejado["__Data Operacional"] = ""

resumo_resultado = (
    resultado
    .groupby("Chave Atendimento", dropna=False)
    .agg(
        Ticket_resultado=("Ticket Jira", "first"),
        Placa_resultado=("Placa", "first"),
        OS_resultado=("OS", juntar_unicos),
        Oficina_resultado=("Oficina", "first"),
        Tipo_resultado=("Tipo de Atividade", "first"),
        Status_resultado=("Status da Atividade", juntar_unicos),
        Razao_improdutiva=(
            "Razão da Improdutiva",
            juntar_unicos,
        ),
        Observacao_tecnico_improdutiva=(
            "Observação do Técnico (Improdutiva)",
            juntar_unicos,
        ),
        Qtd_resultado=("Chave Atendimento", "size"),
    )
    .reset_index()
)

resumo_planejado = (
    planejado
    .groupby("Chave Atendimento", dropna=False)
    .agg(
        Ticket_planejado=("Ticket Jira", "first"),
        Placa_planejada=("Placa", "first"),
        OS_planejada=("OS", juntar_unicos),
        Oficina_planejada=("Oficina", "first"),
        Tipo_planejado=("Tipo de Atividade", "first"),
        Status_planejado=("Status da Atividade", juntar_unicos),
        Primeira_aparicao_data=(
            "__Primeira Aparição Data",
            "first",
        ),
        Data_operacional_planejada=(
            "__Data Operacional",
            "first",
        ),
        Ativa_planejamento=(
            "__Ativa no Planejamento",
            "max",
        ),
        Qtd_planejada=("Chave Atendimento", "size"),
    )
    .reset_index()
)

conciliacao = resumo_planejado.merge(
    resumo_resultado,
    on="Chave Atendimento",
    how="outer",
    indicator=True,
)

# Auditoria de substituição de OS:
# se a OS planejada não aparece pelo mesmo atendimento, procuramos
# outra OS no resultado com o MESMO Ticket Jira + MESMA placa.
# Nessa situação, não declaramos no-show automaticamente.
def chave_ticket_placa(ticket, placa) -> str:
    ticket_norm = normalizar_texto(ticket)
    placa_norm = normalizar_texto(placa)

    if not ticket_norm or not placa_norm:
        return ""

    return f"{ticket_norm}||{placa_norm}"

chaves_resultado_ticket_placa = set()

for _, item in resumo_resultado.iterrows():
    chave = chave_ticket_placa(
        item.get("Ticket_resultado", ""),
        item.get("Placa_resultado", ""),
    )
    if chave:
        chaves_resultado_ticket_placa.add(chave)

def encontrou_substituicao_ticket_placa(linha) -> bool:
    if linha.get("_merge") != "left_only":
        return False

    chave = chave_ticket_placa(
        linha.get("Ticket_planejado", ""),
        linha.get("Placa_planejada", ""),
    )

    return bool(
        chave
        and chave in chaves_resultado_ticket_placa
    )

conciliacao["Possível substituição de OS"] = conciliacao.apply(
    encontrou_substituicao_ticket_placa,
    axis=1,
)

def data_referencia_linha(linha) -> str | None:
    data_operacional = converter_data_operacional(
        linha.get("Data_operacional_planejada", "")
    )
    return data_operacional

def usar_regra_nova(linha) -> bool:
    data_operacional = data_referencia_linha(linha)

    if not data_operacional:
        return False

    return data_operacional >= DATA_CORTE_NOVA_REGRA

def eh_agendada_nova(linha) -> bool:
    if linha.get("_merge") == "right_only":
        return False

    if not bool(linha.get("Ativa_planejamento", False)):
        return False

    primeira = converter_data_operacional(
        linha.get("Primeira_aparicao_data", "")
    )
    data_operacional = data_referencia_linha(linha)

    if not primeira or not data_operacional:
        return False

    return primeira < data_operacional

def eh_agendada_historica(linha) -> bool:
    # Na regra antiga, toda manutenção válida presente no planejado
    # era considerada agendada, independentemente de primeira aparição.
    if linha.get("_merge") == "right_only":
        return False

    status_planejado = linha.get("Status_planejado", "")

    if status_cancelado(status_planejado):
        return False

    return True

def eh_agendada(linha) -> bool:
    if usar_regra_nova(linha):
        return eh_agendada_nova(linha)

    return eh_agendada_historica(linha)

conciliacao["Origem Agendamento"] = conciliacao.apply(
    lambda linha: (
        "Agendada"
        if eh_agendada(linha)
        else "Extra / encaixe"
    ),
    axis=1,
)

def classificar(linha) -> str:
    origem_merge = linha["_merge"]
    status_resultado = linha.get("Status_resultado", "")
    status_planejado = linha.get("Status_planejado", "")
    agendada = linha["Origem Agendamento"] == "Agendada"
    ativa = bool(linha.get("Ativa_planejamento", False))
    nova_regra = usar_regra_nova(linha)

    # Histórico antigo preserva a lógica anterior.
    if not nova_regra:
        if origem_merge == "left_only":
            if status_cancelado(status_planejado):
                return "Cancelada no agendamento"
            if bool(
                linha.get(
                    "Possível substituição de OS",
                    False,
                )
            ):
                return "Possível substituição de OS"
            return "No-show"

        if origem_merge == "right_only":
            if status_improdutivo(status_resultado):
                return "Improdutiva extra"
            if status_cancelado(status_resultado):
                return "Cancelada extra"
            if status_executado(status_resultado):
                return "Executada extra"
            return "Evento extra"

        if status_cancelado(status_planejado):
            return "Cancelada no agendamento"

        if status_improdutivo(status_resultado):
            return "Improdutiva agendada"

        if status_cancelado(status_resultado):
            return "Cancelada"

        if status_executado(status_resultado):
            return "Executada agendada"

        return "Status intermediário agendado"

    # Nova regra, a partir da data de corte.
    if origem_merge == "left_only" and not ativa:
        return "Retirada do agendamento"

    if (
        origem_merge in {"left_only", "both"}
        and ativa
        and status_cancelado(status_planejado)
    ):
        return "Cancelada no agendamento"

    if origem_merge == "left_only":
        if agendada:
            if bool(
                linha.get(
                    "Possível substituição de OS",
                    False,
                )
            ):
                return "Possível substituição de OS"
            return "No-show"
        return "Encaixe não realizado"

    if origem_merge == "right_only":
        if status_improdutivo(status_resultado):
            return "Improdutiva extra"
        if status_cancelado(status_resultado):
            return "Cancelada extra"
        if status_executado(status_resultado):
            return "Executada extra"
        return "Evento extra"

    if status_improdutivo(status_resultado):
        return (
            "Improdutiva agendada"
            if agendada
            else "Improdutiva extra"
        )

    if status_cancelado(status_resultado):
        return (
            "Cancelada"
            if agendada
            else "Cancelada extra"
        )

    if status_executado(status_resultado):
        return (
            "Executada agendada"
            if agendada
            else "Executada extra"
        )

    return (
        "Status intermediário agendado"
        if agendada
        else "Status intermediário extra"
    )

conciliacao["Classificação"] = conciliacao.apply(
    classificar,
    axis=1,
)

def explicar_classificacao(linha) -> str:
    classificacao = linha.get("Classificação", "")
    status_planejado = texto_limpo(
        linha.get("Status_planejado", "")
    )
    status_resultado = texto_limpo(
        linha.get("Status_resultado", "")
    )
    primeira = texto_limpo(
        linha.get("Primeira_aparicao_data", "")
    )
    data_operacional = texto_limpo(
        linha.get("Data_operacional_planejada", "")
    )
    nova_regra = usar_regra_nova(linha)

    if not nova_regra:
        return (
            "Histórico anterior à data de corte: classificação "
            "preservada pela lógica antiga do painel."
        )

    if classificacao == "Executada agendada":
        return (
            f"Manutenção já estava agendada antes de {data_operacional} "
            f"(primeira aparição: {primeira}) e foi executada."
        )
    if classificacao == "Executada extra":
        return (
            "Manutenção não tinha prova de agendamento para essa data "
            "antes do início do dia e foi executada como extra/encaixe."
        )
    if classificacao == "Improdutiva agendada":
        return (
            "Manutenção já estava agendada antes do dia e terminou "
            f"improdutiva/não concluída: {status_resultado}"
        )
    if classificacao == "Improdutiva extra":
        return (
            "Manutenção extra/encaixe terminou improdutiva/não concluída: "
            f"{status_resultado}"
        )
    if classificacao == "Cancelada":
        return (
            "Manutenção estava agendada válida e apareceu cancelada "
            f"posteriormente no resultado: {status_resultado}"
        )
    if classificacao == "Cancelada extra":
        return (
            "Cancelamento de manutenção sem prova de agendamento "
            "anterior para essa mesma data."
        )
    if classificacao == "Cancelada no agendamento":
        return (
            "A manutenção já estava cancelada na fotografia vigente "
            f"do agendamento: {status_planejado}"
        )
    if classificacao == "Possível substituição de OS":
        return (
            "A OS agendada não apareceu pelo mesmo atendimento, mas "
            "foi localizada outra OS no resultado com o mesmo Ticket "
            "Jira + mesma placa. O caso foi retirado do no-show para "
            "auditoria de possível troca/substituição de OS."
        )
    if classificacao == "No-show":
        return (
            "Manutenção estava agendada antes do dia, a própria OS não "
            "apareceu no resultado e nenhuma outra OS com o mesmo Ticket "
            "Jira + mesma placa foi localizada. Classificada como "
            "no-show provável."
        )
    if classificacao == "Encaixe não realizado":
        return (
            "A manutenção surgiu no próprio dia como extra/encaixe "
            "e não apareceu no resultado; não conta como no-show."
        )
    if classificacao == "Retirada do agendamento":
        return (
            "A OS apareceu em fotografia anterior, mas não está mais "
            "no agendamento vigente dessa data."
        )

    return (
        "Status não reconhecido pelas regras principais. "
        f"Planejado: {status_planejado}; resultado: {status_resultado}."
    )

conciliacao["Motivo da Classificação"] = conciliacao.apply(
    explicar_classificacao,
    axis=1,
)

conciliacao["Regra Aplicada"] = conciliacao.apply(
    lambda linha: (
        "Nova regra"
        if usar_regra_nova(linha)
        else "Regra histórica"
    ),
    axis=1,
)

conciliacao["Troca de OS"] = conciliacao.apply(
    lambda linha: (
        "Sim"
        if (
            linha["_merge"] == "both"
            and texto_limpo(
                linha.get("OS_planejada", "")
            )
            != texto_limpo(
                linha.get("OS_resultado", "")
            )
        )
        else "Não"
    ),
    axis=1,
)

conciliacao["Oficina"] = conciliacao[
    "Oficina_planejada"
].fillna(conciliacao["Oficina_resultado"])

conciliacao["Ticket"] = conciliacao[
    "Ticket_planejado"
].fillna(conciliacao["Ticket_resultado"])

conciliacao["Placa"] = conciliacao[
    "Placa_planejada"
].fillna(conciliacao["Placa_resultado"])

conciliacao["Tipo de Serviço"] = conciliacao[
    "Tipo_planejado"
].fillna(conciliacao["Tipo_resultado"])

return conciliacao

def converter_data_operacional(valor) -> str | None:
texto = texto_limpo(valor)

if not texto:
    return None

for dayfirst in (True, False):
    try:
        data_convertida = pd.to_datetime(
            texto,
            dayfirst=dayfirst,
            errors="raise",
        )
        return data_convertida.date().isoformat()
    except Exception:
        pass

return None

def criar_chaves(df: pd.DataFrame) -> pd.DataFrame:
df = df.copy()

for coluna in ["Ticket Jira", "Placa", "OS"]:
    if coluna not in df.columns:
        df[coluna] = ""

    df[coluna] = df[coluna].apply(texto_limpo)

df["Chave Ticket"] = df["Ticket Jira"].apply(normalizar_texto)
df["Chave Placa"] = df["Placa"].apply(normalizar_texto)
df["Chave OS"] = df["OS"].apply(normalizar_texto)

# Os indicadores do painel são baseados em OS.
# Quando houver OS, ela será a chave principal. A regra anterior
# usava Ticket + Placa e podia agrupar OS diferentes.
possui_os = df["Chave OS"] != ""

df["Chave Atendimento"] = ""

df.loc[possui_os, "Chave Atendimento"] = (
    "OS|"
    + df.loc[possui_os, "Chave OS"]
    + "|"
    + df.loc[possui_os, "Chave Placa"]
)

sem_os = ~possui_os

df.loc[sem_os, "Chave Atendimento"] = (
    "TICKET|"
    + df.loc[sem_os, "Chave Ticket"]
    + "|"
    + df.loc[sem_os, "Chave Placa"]
)

sem_identificador = (
    (df["Chave Ticket"] == "")
    & (df["Chave OS"] == "")
    & (df["Chave Placa"] == "")
)

df.loc[sem_identificador, "Chave Atendimento"] = (
    "LINHA|" + df.index[sem_identificador].astype(str)
)

return df

def enriquecer_com_cadastro(
base: pd.DataFrame,
cadastro: pd.DataFrame,
) -> pd.DataFrame:
resultado = base.copy()

if "Oficina" not in resultado.columns:
    resultado["Oficina"] = ""

resultado["Chave Oficina"] = resultado["Oficina"].apply(
    normalizar_texto
)

colunas = [
    "Chave Oficina",
    "Cidade-base",
    "UF-base",
    "Consultor",
    "WhatsApp",
    "Prioridade",
]

resultado = resultado.merge(
    cadastro[colunas].drop_duplicates(
        subset=["Chave Oficina"]
    ),
    on="Chave Oficina",
    how="left",
)

resultado["Consultor"] = resultado["Consultor"].fillna(
    "Não definido"
)
resultado["Cidade-base"] = resultado["Cidade-base"].fillna("")
resultado["UF-base"] = resultado["UF-base"].fillna("")
resultado["WhatsApp"] = resultado["WhatsApp"].fillna("")
resultado["Prioridade"] = resultado["Prioridade"].fillna("Normal")

return resultado

def exigir_supabase() -> Client:
if SUPABASE is None:
st.error(ERRO_SUPABASE or "Supabase não conectado.")
st.stop()

return SUPABASE

def filtrar_somente_manutencoes(df: pd.DataFrame) -> pd.DataFrame:
"""
Mantém somente atividades cujo Tipo de Atividade contenha
a palavra MANUTEN, cobrindo manutenção, manutenções,
manutenção corretiva, preventiva e demais variações.
"""
if df is None or df.empty:
return pd.DataFrame(columns=df.columns if df is not None else [])

if "Tipo de Atividade" not in df.columns:
    raise ValueError(
        "A base não possui a coluna 'Tipo de Atividade'. "
        "Não foi possível filtrar somente manutenções."
    )

base = df.copy()

mascara = base["Tipo de Atividade"].apply(
    lambda valor: "MANUTEN" in normalizar_texto(valor)
)

return base[mascara].copy().reset_index(drop=True)

def juntar_unicos(valores) -> str:
itens = sorted(
{
texto_limpo(valor)
for valor in valores
if texto_limpo(valor)
}
)
return " | ".join(itens)

def listar_bases() -> pd.DataFrame:
registros = buscar_todos(
"bases_importadas",
ordem="data_operacional",
desc=True,
)
return pd.DataFrame(registros)

def normalizar_texto(valor) -> str:
texto = texto_limpo(valor).upper()
texto = unicodedata.normalize("NFKD", texto)
texto = "".join(
caractere
for caractere in texto
if not unicodedata.combining(caractere)
)
texto = re.sub(r"\s+", " ", texto)
return texto.strip()

def status_cancelado(valor) -> bool:
return "CANCEL" in status_normalizado(valor)

def status_executado(valor) -> bool:
status = status_normalizado(valor)
return any(
termo in status
for termo in [
"CONCLUID",
"EXECUTAD",
"FINALIZAD",
"COMPLET",
"REALIZAD",
]
)

def status_improdutivo(valor) -> bool:
status = status_normalizado(valor)
return any(
termo in status
for termo in [
"NAO CONCLUIDO",
"NAO CONCLUIDA",
"IMPRODUTIVO",
"IMPRODUTIVA",
"SEM SUCESSO",
]
)

def status_normalizado(valor) -> str:
return normalizar_texto(valor)

def texto_limpo(valor) -> str:
if valor is None:
return ""

try:
    if pd.isna(valor):
        return ""
except (TypeError, ValueError):
    pass

texto = str(valor).strip()

if texto.lower() in {"nan", "none", "null", "nat"}:
    return ""

return texto




def calcular_indicadores(conciliacao: pd.DataFrame) -> dict:
agendadas_validas = {
"Executada agendada",
"Improdutiva agendada",
"Cancelada",
"No-show",
"Status intermediário agendado",
}

manutencoes_agendadas = int(
    conciliacao["Classificação"].isin(
        agendadas_validas
    ).sum()
)

agendadas_executadas = int(
    (
        conciliacao["Classificação"]
        == "Executada agendada"
    ).sum()
)

executadas_extras = int(
    (
        conciliacao["Classificação"]
        == "Executada extra"
    ).sum()
)

improdutivas_agendadas = int(
    (
        conciliacao["Classificação"]
        == "Improdutiva agendada"
    ).sum()
)

improdutivas_extras = int(
    (
        conciliacao["Classificação"]
        == "Improdutiva extra"
    ).sum()
)

improdutivas = (
    improdutivas_agendadas
    + improdutivas_extras
)

canceladas = int(
    (conciliacao["Classificação"] == "Cancelada").sum()
)

no_show = int(
    (conciliacao["Classificação"] == "No-show").sum()
)

mci = (
    agendadas_executadas
    / manutencoes_agendadas
    * 100
    if manutencoes_agendadas
    else 0.0
)

base_md = (
    agendadas_executadas
    + executadas_extras
    + improdutivas
)

md = (
    improdutivas / base_md * 100
    if base_md
    else 0.0
)

return {
    "Planejadas": manutencoes_agendadas,
    "Executadas planejadas": agendadas_executadas,
    "Improdutivas": improdutivas,
    "Improdutivas agendadas": improdutivas_agendadas,
    "Improdutivas extras": improdutivas_extras,
    "Canceladas": canceladas,
    "No-show": no_show,
    "Possíveis substituições de OS": int(
        (
            conciliacao["Classificação"]
            == "Possível substituição de OS"
        ).sum()
    ),
    "Executadas extras": executadas_extras,
    "MCI": mci,
    "MD": md,
    "Índice no-show": (
        no_show / manutencoes_agendadas * 100
        if manutencoes_agendadas
        else 0.0
    ),
    "Índice cancelamento": (
        canceladas / manutencoes_agendadas * 100
        if manutencoes_agendadas
        else 0.0
    ),
    "Execução total": (
        (
            agendadas_executadas
            + executadas_extras
        )
        / manutencoes_agendadas
        * 100
        if manutencoes_agendadas
        else 0.0
    ),
}




def limpar_telefone(valor) -> str:
numeros = re.sub(r"\D", "", texto_limpo(valor))

if numeros.startswith("0") and len(numeros) > 10:
    numeros = numeros[1:]

if numeros and not numeros.startswith("55"):
    numeros = f"55{numeros}"

return numeros

def carregar_resposta_mais_recente_follow(follow_id: int) -> dict:
cliente = exigir_supabase()

resposta = (
    cliente.table("follow_respostas")
    .select("*")
    .eq("follow_id", follow_id)
    .order("respondido_em", desc=True)
    .limit(1)
    .execute()
)

if not resposta.data:
    return {}

return resposta.data[0]
=========================================================
FOLLOW INTEGRADO AO PORTAL DA OFICINA
=========================================================

MOTIVOS_IMPEDIMENTO_PORTAL = [
"Veículo indisponível",
"Cliente solicitou alteração da data",
"Falta de equipamento ou ferramenta",
"Falta de peça ou insumo",
"Problema técnico sem solução",
"Técnico/equipe indisponível",
"Oficina sem capacidade para a data",
"Dificuldade de acesso ou deslocamento",
"Dados/OS insuficientes para executar",
"Outro",
]

def buscar_follow_pendentes_portal() -> pd.DataFrame:
registros = buscar_todos(
"follow_contatos",
ordem="data_manutencao",
desc=False,
)

if not registros:
    return pd.DataFrame()

df = pd.DataFrame(registros)

if df.empty or "oficina" not in df.columns:
    return pd.DataFrame()

df["oficina_norm"] = (
    df["oficina"]
    .fillna("")
    .astype(str)
    .str.strip()
    .str.upper()
)

return df[
    df["oficina_norm"] == OFICINA_PORTAL.upper()
].copy()

def carregar_resposta_follow_portal(follow_id: int) -> dict:
resposta = (
cliente.table("follow_respostas")
.select("*")
.eq("follow_id", follow_id)
.order("respondido_em", desc=True)
.limit(1)
.execute()
)

if not resposta.data:
    return {}

return resposta.data[0]

def enviar_resposta_follow_portal(
follow: dict,
nome_respondente: str,
equipamentos: str,
veiculo: str,
capacidade: str,
tem_impedimento: bool,
motivos: list[str],
os_afetadas: list[str],
observacao: str,
previsao: str,
) -> None:
agora = datetime.now().astimezone().isoformat()

cliente.table("follow_respostas").insert(
    {
        "follow_id": int(follow["id"]),
        "token": str(follow["token"]),
        "nome_respondente": nome_respondente,
        "equipamentos_ok": equipamentos == "Sim",
        "veiculo_disponivel": veiculo,
        "capacidade_ok": capacidade == "Sim",
        "tem_impedimento": tem_impedimento,
        "motivos": motivos,
        "os_afetadas": os_afetadas,
        "observacao": observacao,
        "previsao_solucao": previsao,
        "respondido_em": agora,
    }
).execute()

cliente.table("follow_contatos").update(
    {
        "status": "Respondido",
        "respondido_em": agora,
        "tem_impedimento": tem_impedimento,
        "status_resposta": (
            "Com impedimento"
            if tem_impedimento
            else "Sem impedimento"
        ),
        "ultima_atualizacao": agora,
    }
).eq(
    "id",
    int(follow["id"]),
).execute()




def gerar_follows_automaticos_portal() -> dict:
"""
Gera o Follow diretamente da tabela atividades_planejadas.

Esta versão não depende de bases_importadas para descobrir as datas.
Ela lê o planejamento vigente salvo no Supabase, filtra:
- somente manutenções;
- somente a oficina do portal;
- somente registros ativos;
- exclui canceladas;
- somente hoje e datas futuras.
"""
hoje = date.today()

registros = buscar_todos(
    "atividades_planejadas",
    ordem="data_operacional",
    desc=False,
)

if not registros:
    return {
        "criados": 0,
        "atualizados": 0,
        "pendentes": 0,
        "datas_detectadas": [],
        "os_detectadas": 0,
    }

linhas = []

for registro in registros:
    dados = dict(registro.get("dados") or {})

    linha = {
        "data_operacional": str(
            registro.get("data_operacional") or ""
        ),
        "chave_atendimento": texto_limpo(
            registro.get("chave_atendimento", "")
        ),
        "os": texto_limpo(
            registro.get("os", "")
        ),
        "oficina": texto_limpo(
            registro.get("oficina", "")
        ),
        "tipo_atividade": texto_limpo(
            registro.get("tipo_atividade", "")
        ),
        "status_atividade": texto_limpo(
            registro.get("status_atividade", "")
        ),
        "ativa_no_planejamento": bool(
            registro.get(
                "ativa_no_planejamento",
                True,
            )
        ),
    }

    # Fallback para dados históricos armazenados no JSON.
    if not linha["oficina"]:
        linha["oficina"] = texto_limpo(
            dados.get("Oficina", "")
        )

    if not linha["tipo_atividade"]:
        linha["tipo_atividade"] = texto_limpo(
            dados.get("Tipo de Atividade", "")
        )

    if not linha["status_atividade"]:
        linha["status_atividade"] = texto_limpo(
            dados.get("Status da Atividade", "")
        )

    if not linha["os"]:
        linha["os"] = texto_limpo(
            dados.get("OS", "")
        )

    linhas.append(linha)

df = pd.DataFrame(linhas)

if df.empty:
    return {
        "criados": 0,
        "atualizados": 0,
        "pendentes": 0,
        "datas_detectadas": [],
        "os_detectadas": 0,
    }

df["data_dt"] = pd.to_datetime(
    df["data_operacional"],
    errors="coerce",
).dt.date

# Somente datas atuais/futuras.
df = df[
    df["data_dt"].notna()
    & (df["data_dt"] >= hoje)
].copy()

# Somente registros ainda vigentes no planejamento.
df = df[
    df["ativa_no_planejamento"] == True
].copy()

# Somente manutenções.
df = df[
    df["tipo_atividade"].apply(
        lambda valor: (
            "MANUTEN" in normalizar_texto(valor)
        )
    )
].copy()

# Exclui canceladas.
df = df[
    ~df["status_atividade"].apply(
        status_cancelado
    )
].copy()

# Somente a oficina deste portal.
df = df[
    df["oficina"].apply(
        normalizar_texto
    ) == normalizar_texto(
        OFICINA_PORTAL
    )
].copy()

if df.empty:
    return {
        "criados": 0,
        "atualizados": 0,
        "pendentes": 0,
        "datas_detectadas": [],
        "os_detectadas": 0,
    }

# Evita duplicidade da mesma atividade.
df["chave_unica"] = df.apply(
    lambda linha: (
        texto_limpo(
            linha["chave_atendimento"]
        )
        or (
            f"{linha['data_operacional']}|"
            f"{texto_limpo(linha['os'])}"
        )
    ),
    axis=1,
)

df = df.drop_duplicates(
    subset=["data_operacional", "chave_unica"],
    keep="last",
)

cadastro = carregar_oficinas()

consultor = "Não definido"
telefone = ""
chave_oficina = normalizar_texto(
    OFICINA_PORTAL
)

if not cadastro.empty:
    cadastro_oficina = cadastro[
        cadastro["Oficina"].apply(
            normalizar_texto
        ) == normalizar_texto(
            OFICINA_PORTAL
        )
    ]

    if not cadastro_oficina.empty:
        cad = cadastro_oficina.iloc[0]

        consultor = (
            texto_limpo(
                cad.get("Consultor", "")
            )
            or "Não definido"
        )
        telefone = texto_limpo(
            cad.get("WhatsApp", "")
        )
        chave_oficina = (
            texto_limpo(
                cad.get("Chave Oficina", "")
            )
            or chave_oficina
        )

existentes_raw = buscar_todos(
    "follow_contatos",
    ordem="data_manutencao",
    desc=False,
)
existentes = pd.DataFrame(
    existentes_raw
)

criados = 0
atualizados = 0

for data_operacional, grupo in df.groupby(
    "data_operacional"
):
    os_lista = sorted(
        {
            texto_limpo(v)
            for v in grupo["os"]
            if texto_limpo(v)
        }
    )

    quantidade = len(grupo)

    existente = None

    if not existentes.empty:
        mascara = (
            existentes["data_manutencao"]
            .astype(str)
            .eq(str(data_operacional))
        )

        if "oficina" in existentes.columns:
            mascara = mascara & (
                existentes["oficina"]
                .fillna("")
                .astype(str)
                .apply(normalizar_texto)
                .eq(
                    normalizar_texto(
                        OFICINA_PORTAL
                    )
                )
            )

        encontrados = existentes[
            mascara
        ]

        if not encontrados.empty:
            existente = (
                encontrados
                .sort_values("id")
                .iloc[-1]
                .to_dict()
            )

    agora = (
        datetime.now()
        .astimezone()
        .isoformat()
    )

    mensagem = (
        f"Olá! A {OFICINA_PORTAL} possui "
        f"{quantidade} manutenção(ões) "
        f"planejada(s) para "
        f"{pd.to_datetime(data_operacional).strftime('%d/%m/%Y')}. "
        "Acesse o Portal da Oficina e confirme o Follow."
    )

    if existente:
        payload = {
            "qtd_agendadas": quantidade,
            "os_agendadas": os_lista,
            "telefone": telefone,
            "mensagem": mensagem,
            "ultima_atualizacao": agora,
        }

        # Só reabre como pendente se ainda não houve resposta.
        if not existente.get(
            "respondido_em"
        ):
            payload["status"] = "Pendente"

        cliente.table(
            "follow_contatos"
        ).update(
            payload
        ).eq(
            "id",
            int(existente["id"]),
        ).execute()

        atualizados += 1

    else:
        cliente.table(
            "follow_contatos"
        ).insert(
            {
                "token": str(uuid.uuid4()),
                "data_follow": hoje.isoformat(),
                "data_manutencao": (
                    data_operacional
                ),
                "chave_oficina": chave_oficina,
                "oficina": OFICINA_PORTAL,
                "consultor": consultor,
                "telefone": telefone,
                "qtd_agendadas": quantidade,
                "os_agendadas": os_lista,
                "mensagem": mensagem,
                "status": "Pendente",
                "preparado_em": agora,
                "ultima_atualizacao": agora,
            }
        ).execute()

        criados += 1

follows = buscar_follow_pendentes_portal()

pendentes = 0

if not follows.empty:
    follows["data_dt"] = pd.to_datetime(
        follows["data_manutencao"],
        errors="coerce",
    ).dt.date

    pendentes = int(
        (
            follows["data_dt"].notna()
            & (follows["data_dt"] >= hoje)
            & (follows["respondido_em"].isna() | follows["respondido_em"].astype(str).str.strip().str.lower().isin(["", "nan", "none", "nat"]))
        ).sum()
    )

return {
    "criados": criados,
    "atualizados": atualizados,
    "pendentes": pendentes,
    "datas_detectadas": sorted(
        df["data_operacional"]
        .astype(str)
        .unique()
        .tolist()
    ),
    "os_detectadas": len(df),
}

def resumo_alerta_follow_portal() -> tuple[int, int]:
"""
Retorna:
(quantidade de datas com Follow pendente,
quantidade total de manutenções aguardando confirmação)
"""
follows = buscar_follow_pendentes_portal()

if follows.empty:
    return 0, 0

hoje = date.today()

follows["data_dt"] = pd.to_datetime(
    follows["data_manutencao"],
    errors="coerce",
).dt.date

pendentes = follows[
    follows["data_dt"].notna()
    & (follows["data_dt"] >= hoje)
    & (follows["respondido_em"].isna() | follows["respondido_em"].astype(str).str.strip().str.lower().isin(["", "nan", "none", "nat"]))
].copy()

if pendentes.empty:
    return 0, 0

return (
    len(pendentes),
    int(
        pendentes["qtd_agendadas"]
        .fillna(0)
        .astype(int)
        .sum()
    ),
)




def carregar_respostas_os_follow(
follow_id: int,
) -> pd.DataFrame:
resposta = (
cliente.table("follow_respostas_os")
.select("*")
.eq("follow_id", follow_id)
.order("os_numero")
.execute()
)

return pd.DataFrame(
    resposta.data or []
)

def salvar_resposta_os_follow(
follow: dict,
os_numero: str,
nome_respondente: str,
equipamentos: str,
veiculo: str,
capacidade: str,
tem_impedimento: bool,
motivos: list[str],
observacao: str,
previsao: str,
) -> None:
agora = datetime.now().astimezone().isoformat()

payload = {
    "follow_id": int(follow["id"]),
    "os_numero": texto_limpo(os_numero),
    "nome_respondente": nome_respondente,
    "equipamentos_ok": equipamentos == "Sim",
    "veiculo_disponivel": veiculo,
    "capacidade_ok": capacidade == "Sim",
    "tem_impedimento": tem_impedimento,
    "motivos": motivos,
    "observacao": observacao,
    "previsao_solucao": previsao,
    "respondido_em": agora,
    "atualizado_em": agora,
}

(
    cliente.table("follow_respostas_os")
    .upsert(
        payload,
        on_conflict="follow_id,os_numero",
    )
    .execute()
)

atualizar_resumo_follow_por_os(
    follow
)

def confirmar_todas_os_ok(
follow: dict,
os_lista: list[str],
nome_respondente: str,
) -> None:
agora = datetime.now().astimezone().isoformat()

registros = []

for os_numero in os_lista:
    registros.append(
        {
            "follow_id": int(follow["id"]),
            "os_numero": texto_limpo(os_numero),
            "nome_respondente": nome_respondente,
            "equipamentos_ok": True,
            "veiculo_disponivel": "Sim",
            "capacidade_ok": True,
            "tem_impedimento": False,
            "motivos": [],
            "observacao": "",
            "previsao_solucao": "",
            "respondido_em": agora,
            "atualizado_em": agora,
        }
    )

if registros:
    (
        cliente.table("follow_respostas_os")
        .upsert(
            registros,
            on_conflict="follow_id,os_numero",
        )
        .execute()
    )

atualizar_resumo_follow_por_os(
    follow
)

def atualizar_resumo_follow_por_os(
follow: dict,
) -> None:
respostas = carregar_respostas_os_follow(
int(follow["id"])
)

os_lista = [
    texto_limpo(v)
    for v in (follow.get("os_agendadas") or [])
    if texto_limpo(v)
]

total = len(os_lista)

if respostas.empty:
    respondidas = 0
    com_impedimento = 0
    sem_impedimento = 0
else:
    respostas = respostas[
        respostas["os_numero"]
        .astype(str)
        .isin(os_lista)
    ].copy()

    respondidas = int(
        respostas["os_numero"]
        .astype(str)
        .nunique()
    )

    com_impedimento = int(
        respostas[
            respostas["tem_impedimento"] == True
        ]["os_numero"]
        .astype(str)
        .nunique()
    )

    sem_impedimento = max(
        respondidas - com_impedimento,
        0,
    )

faltantes = max(
    total - respondidas,
    0,
)

agora = datetime.now().astimezone().isoformat()

if total and faltantes == 0:
    status = "Respondido"

    if com_impedimento:
        status_resposta = (
            f"{com_impedimento} com impedimento · "
            f"{sem_impedimento} sem impedimento"
        )
        tem_impedimento = True
    else:
        status_resposta = "Sem impedimento"
        tem_impedimento = False

    respondido_em = agora

elif respondidas > 0:
    status = "Parcial"
    status_resposta = (
        f"{respondidas}/{total} OS respondidas"
    )
    tem_impedimento = (
        com_impedimento > 0
    )
    respondido_em = None

else:
    status = "Pendente"
    status_resposta = "Pendente"
    tem_impedimento = False
    respondido_em = None

cliente.table("follow_contatos").update(
    {
        "status": status,
        "status_resposta": status_resposta,
        "tem_impedimento": tem_impedimento,
        "respondido_em": respondido_em,
        "ultima_atualizacao": agora,
    }
).eq(
    "id",
    int(follow["id"]),
).execute()

# Compatibilidade com o painel de gestão atual:
# sempre que houver respostas individuais, gravamos também um resumo
# na tabela legada follow_respostas.
if respondidas > 0:
    motivos = []
    os_afetadas = []
    observacoes = []

    if not respostas.empty:
        for _, item in respostas.iterrows():
            if bool(
                item.get(
                    "tem_impedimento",
                    False,
                )
            ):
                os_afetadas.append(
                    texto_limpo(
                        item.get(
                            "os_numero",
                            "",
                        )
                    )
                )

            motivos_item = (
                item.get("motivos")
                or []
            )
            motivos.extend(
                [
                    texto_limpo(v)
                    for v in motivos_item
                    if texto_limpo(v)
                ]
            )

            obs = texto_limpo(
                item.get(
                    "observacao",
                    "",
                )
            )
            if obs:
                observacoes.append(
                    f"{texto_limpo(item.get('os_numero',''))}: {obs}"
                )

    cliente.table(
        "follow_respostas"
    ).insert(
        {
            "follow_id": int(follow["id"]),
            "token": str(follow["token"]),
            "nome_respondente": "Resposta individual por OS",
            "equipamentos_ok": bool(
                respostas["equipamentos_ok"]
                .fillna(False)
                .all()
            )
            if not respostas.empty
            else False,
            "veiculo_disponivel": (
                "Sim"
                if (
                    not respostas.empty
                    and (
                        respostas["veiculo_disponivel"]
                        .fillna("")
                        == "Sim"
                    ).all()
                )
                else "Parcial"
            ),
            "capacidade_ok": bool(
                respostas["capacidade_ok"]
                .fillna(False)
                .all()
            )
            if not respostas.empty
            else False,
            "tem_impedimento": (
                com_impedimento > 0
            ),
            "motivos": sorted(
                set(motivos)
            ),
            "os_afetadas": sorted(
                set(
                    [
                        v
                        for v in os_afetadas
                        if v
                    ]
                )
            ),
            "observacao": " | ".join(
                observacoes
            ),
            "previsao_solucao": "",
            "respondido_em": agora,
        }
    ).execute()




def exibir_follow_portal() -> None:
st.markdown("### 📞 Follow de Manutenções")
st.caption(
"Cada OS deve ser confirmada individualmente. "
"Se todas estiverem OK, use o botão de confirmação em lote."
)

follows = buscar_follow_pendentes_portal()

if follows.empty:
    st.success(
        "Você não possui Follow disponível no momento."
    )
    return

hoje = date.today()

follows["data_dt"] = pd.to_datetime(
    follows["data_manutencao"],
    errors="coerce",
).dt.date

pendentes = follows[
    follows["data_dt"].notna()
    & (follows["data_dt"] >= hoje)
].copy()

if pendentes.empty:
    st.info(
        "Não há Follow para datas atuais ou futuras."
    )
    return

st.markdown("#### Pendências para confirmação")

planejamento = carregar_planejamento_futuro_portal()

for _, row in pendentes.sort_values(
    "data_dt"
).iterrows():
    follow = row.to_dict()

    data_txt = pd.to_datetime(
        follow["data_manutencao"]
    ).strftime("%d/%m/%Y")

    os_lista = [
        texto_limpo(x)
        for x in (
            follow.get("os_agendadas")
            or []
        )
        if texto_limpo(x)
    ]

    respostas = carregar_respostas_os_follow(
        int(follow["id"])
    )

    respondidas = set()

    if not respostas.empty:
        respondidas = set(
            respostas["os_numero"]
            .fillna("")
            .astype(str)
        )

    total = len(os_lista)
    total_respondidas = len(
        [
            os_numero
            for os_numero in os_lista
            if os_numero in respondidas
        ]
    )

    total_impedimentos = 0
    if not respostas.empty:
        total_impedimentos = int(
            respostas[
                (
                    respostas["os_numero"]
                    .astype(str)
                    .isin(os_lista)
                )
                & (
                    respostas[
                        "tem_impedimento"
                    ] == True
                )
            ]["os_numero"]
            .nunique()
        )

    sem_resposta = max(
        total - total_respondidas,
        0,
    )

    with st.container(border=True):
        st.markdown(
            f"### Follow • {data_txt}"
        )

        r1, r2, r3, r4 = st.columns(
            4
        )
        r1.metric(
            "OS previstas",
            total,
        )
        r2.metric(
            "Confirmadas",
            total_respondidas,
        )
        r3.metric(
            "Com impedimento",
            total_impedimentos,
        )
        r4.metric(
            "Sem resposta",
            sem_resposta,
        )

        if sem_resposta == 0 and total > 0:
            if total_impedimentos:
                st.warning(
                    "⚠️ Follow concluído com impedimento(s)."
                )
            else:
                st.success(
                    "✅ Follow concluído. Todas as OS estão confirmadas."
                )
        else:
            st.warning(
                f"⚠️ Ainda existem {sem_resposta} OS "
                "aguardando confirmação."
            )

        st.markdown(
            "#### Confirmação rápida"
        )

        nome_lote = st.text_input(
            "Seu nome para confirmação em lote",
            key=f"nome_lote_{follow['id']}",
        )

        if st.button(
            "✅ Confirmar todas as OS como OK",
            key=f"confirmar_todas_{follow['id']}",
            use_container_width=True,
        ):
            if not nome_lote.strip():
                st.error(
                    "Informe seu nome antes de confirmar."
                )
            elif not os_lista:
                st.error(
                    "Nenhuma OS disponível para confirmação."
                )
            else:
                confirmar_todas_os_ok(
                    follow=follow,
                    os_lista=os_lista,
                    nome_respondente=nome_lote,
                )
                st.success(
                    "Todas as OS foram confirmadas como OK."
                )
                st.rerun()

        st.divider()
        st.markdown(
            "#### Resposta individual por OS"
        )

        for os_numero in os_lista:
            detalhe_os = pd.DataFrame()

            if not planejamento.empty:
                detalhe_os = planejamento[
                    planejamento["OS"]
                    .astype(str)
                    == str(os_numero)
                ].copy()

            cliente_nome = ""
            local = ""
            tipo = "Manutenção"
            status_os = "Pendente"

            if not detalhe_os.empty:
                item = detalhe_os.iloc[0]
                cliente_nome = texto_limpo(
                    item.get("Cliente", "")
                )
                local = texto_limpo(
                    item.get("Local", "")
                )
                tipo = (
                    texto_limpo(
                        item.get(
                            "Tipo de Serviço",
                            "",
                        )
                    )
                    or tipo
                )
                status_os = (
                    texto_limpo(
                        item.get(
                            "Status",
                            "",
                        )
                    )
                    or status_os
                )

            resposta_os = {}

            if not respostas.empty:
                achado = respostas[
                    respostas["os_numero"]
                    .astype(str)
                    == str(os_numero)
                ]

                if not achado.empty:
                    resposta_os = (
                        achado.iloc[-1]
                        .to_dict()
                    )

            respondida = bool(
                resposta_os
            )

            titulo_status = (
                "⚠️ Com impedimento"
                if (
                    respondida
                    and bool(
                        resposta_os.get(
                            "tem_impedimento",
                            False,
                        )
                    )
                )
                else (
                    "✅ Tudo OK"
                    if respondida
                    else "🔴 Aguardando resposta"
                )
            )

            with st.expander(
                f"{os_numero} • {titulo_status}",
                expanded=not respondida,
            ):
                d1, d2 = st.columns(2)

                with d1:
                    st.write(
                        f"**Cliente:** "
                        f"{cliente_nome or 'Não informado'}"
                    )
                    st.write(
                        f"**Tipo:** {tipo}"
                    )

                with d2:
                    st.write(
                        f"**Local:** "
                        f"{local or 'Não informado'}"
                    )
                    st.write(
                        f"**Status OFS:** {status_os}"
                    )

                if respondida:
                    if bool(
                        resposta_os.get(
                            "tem_impedimento",
                            False,
                        )
                    ):
                        st.warning(
                            "Esta OS foi confirmada com impedimento."
                        )
                    else:
                        st.success(
                            "Esta OS foi confirmada sem impedimento."
                        )

                    respondente = texto_limpo(
                        resposta_os.get(
                            "nome_respondente",
                            "",
                        )
                    )
                    if respondente:
                        st.caption(
                            f"Respondido por {respondente}"
                        )

                    motivos_resp = (
                        resposta_os.get("motivos")
                        or []
                    )
                    if motivos_resp:
                        st.write(
                            "**Motivo(s):** "
                            + " | ".join(
                                map(
                                    str,
                                    motivos_resp,
                                )
                            )
                        )

                    obs_resp = texto_limpo(
                        resposta_os.get(
                            "observacao",
                            "",
                        )
                    )
                    if obs_resp:
                        st.write(
                            f"**Observação:** "
                            f"{obs_resp}"
                        )

                    if st.button(
                        "✏️ Alterar resposta desta OS",
                        key=(
                            f"editar_os_"
                            f"{follow['id']}_"
                            f"{os_numero}"
                        ),
                    ):
                        st.session_state[
                            f"editar_follow_os_"
                            f"{follow['id']}_"
                            f"{os_numero}"
                        ] = True
                        st.rerun()

                editar = (
                    not respondida
                    or st.session_state.get(
                        f"editar_follow_os_"
                        f"{follow['id']}_"
                        f"{os_numero}",
                        False,
                    )
                )

                if editar:
                    with st.form(
                        f"form_os_"
                        f"{follow['id']}_"
                        f"{os_numero}"
                    ):
                        nome = st.text_input(
                            "Seu nome",
                            value=texto_limpo(
                                resposta_os.get(
                                    "nome_respondente",
                                    "",
                                )
                            )
                            if respondida
                            else "",
                        )

                        situacao = st.radio(
                            "Situação desta OS",
                            [
                                "✅ Tudo OK",
                                "⚠️ Tenho impedimento",
                            ],
                            index=(
                                1
                                if (
                                    respondida
                                    and bool(
                                        resposta_os.get(
                                            "tem_impedimento",
                                            False,
                                        )
                                    )
                                )
                                else 0
                            ),
                            horizontal=True,
                        )

                        tem_impedimento = (
                            situacao
                            == "⚠️ Tenho impedimento"
                        )

                        equipamentos = st.radio(
                            "Equipamentos/ferramentas disponíveis?",
                            ["Sim", "Não"],
                            horizontal=True,
                            index=(
                                0
                                if (
                                    not respondida
                                    or bool(
                                        resposta_os.get(
                                            "equipamentos_ok",
                                            True,
                                        )
                                    )
                                )
                                else 1
                            ),
                        )

                        veiculo_opcoes = [
                            "Sim",
                            "Não",
                            "Não sei",
                        ]
                        veiculo_atual = texto_limpo(
                            resposta_os.get(
                                "veiculo_disponivel",
                                "Sim",
                            )
                        )
                        if (
                            veiculo_atual
                            not in veiculo_opcoes
                        ):
                            veiculo_atual = "Sim"

                        veiculo = st.radio(
                            "Veículo disponível?",
                            veiculo_opcoes,
                            horizontal=True,
                            index=veiculo_opcoes.index(
                                veiculo_atual
                            ),
                        )

                        capacidade = st.radio(
                            "Capacidade/técnico disponível?",
                            ["Sim", "Não"],
                            horizontal=True,
                            index=(
                                0
                                if (
                                    not respondida
                                    or bool(
                                        resposta_os.get(
                                            "capacidade_ok",
                                            True,
                                        )
                                    )
                                )
                                else 1
                            ),
                        )

                        motivos = []
                        observacao = ""
                        previsao = ""

                        if tem_impedimento:
                            motivos = st.multiselect(
                                "Qual(is) o(s) impedimento(s)?",
                                MOTIVOS_IMPEDIMENTO_PORTAL,
                                default=(
                                    resposta_os.get(
                                        "motivos"
                                    )
                                    or []
                                )
                                if respondida
                                else [],
                            )

                            observacao = st.text_area(
                                "Explique o impedimento",
                                value=texto_limpo(
                                    resposta_os.get(
                                        "observacao",
                                        "",
                                    )
                                )
                                if respondida
                                else "",
                            )

                            previsao = st.text_input(
                                "Previsão de solução",
                                value=texto_limpo(
                                    resposta_os.get(
                                        "previsao_solucao",
                                        "",
                                    )
                                )
                                if respondida
                                else "",
                            )
                        else:
                            observacao = st.text_area(
                                "Observação (opcional)",
                                value=texto_limpo(
                                    resposta_os.get(
                                        "observacao",
                                        "",
                                    )
                                )
                                if respondida
                                else "",
                            )

                        salvar = (
                            st.form_submit_button(
                                "💾 Salvar resposta desta OS",
                                type="primary",
                                use_container_width=True,
                            )
                        )

                    if salvar:
                        if not nome.strip():
                            st.error(
                                "Informe seu nome."
                            )
                        elif (
                            tem_impedimento
                            and not motivos
                        ):
                            st.error(
                                "Selecione ao menos um motivo."
                            )
                        else:
                            salvar_resposta_os_follow(
                                follow=follow,
                                os_numero=os_numero,
                                nome_respondente=nome,
                                equipamentos=equipamentos,
                                veiculo=veiculo,
                                capacidade=capacidade,
                                tem_impedimento=tem_impedimento,
                                motivos=motivos,
                                observacao=observacao,
                                previsao=previsao,
                            )

                            st.session_state.pop(
                                f"editar_follow_os_"
                                f"{follow['id']}_"
                                f"{os_numero}",
                                None,
                            )

                            st.success(
                                f"Resposta da OS {os_numero} salva."
                            )
                            st.rerun()

st.divider()
st.markdown(
    "#### Histórico recente de Follow"
)

historico = follows[
    follows["status"]
    .fillna("")
    .astype(str)
    .isin(
        [
            "Parcial",
            "Respondido",
        ]
    )
].copy()

if historico.empty:
    st.caption(
        "Nenhuma resposta registrada ainda."
    )
else:
    cols = [
        c
        for c in [
            "data_manutencao",
            "qtd_agendadas",
            "status",
            "status_resposta",
            "ultima_atualizacao",
        ]
        if c in historico.columns
    ]

    st.dataframe(
        historico[cols]
        .sort_values(
            "data_manutencao",
            ascending=False,
        ),
        use_container_width=True,
        hide_index=True,
    )

def calcular_indicadores_portal_todos_servicos(
conciliacao: pd.DataFrame,
) -> dict:
classes = conciliacao[
"Classificação"
].fillna("").astype(str)

planejadas_validas = {
    "Executada agendada",
    "Improdutiva agendada",
    "Cancelada",
    "No-show",
    "Status intermediário agendado",
}

planejadas = int(
    classes.isin(
        planejadas_validas
    ).sum()
)

executadas_agendadas = int(
    (
        classes
        == "Executada agendada"
    ).sum()
)

executadas_extras = int(
    (
        classes
        == "Executada extra"
    ).sum()
)

improdutivas_agendadas = int(
    (
        classes
        == "Improdutiva agendada"
    ).sum()
)

improdutivas_extras = int(
    (
        classes
        == "Improdutiva extra"
    ).sum()
)

no_show = int(
    (
        classes
        == "No-show"
    ).sum()
)

canceladas = int(
    (
        classes
        == "Cancelada"
    ).sum()
)

base_executavel = max(
    planejadas - canceladas,
    0,
)

indice_execucao = (
    executadas_agendadas
    / base_executavel
    * 100
    if base_executavel
    else 0.0
)

perdas_agendadas = (
    improdutivas_agendadas
    + no_show
)

indice_perda = (
    perdas_agendadas
    / base_executavel
    * 100
    if base_executavel
    else 0.0
)

if (
    indice_execucao >= 90
    and indice_perda <= 10
):
    nivel = "Excelente"
    simbolo = "🟢"
    mensagem = (
        "Ótimo aproveitamento do planejamento. "
        "Mantenha o ritmo e continue prevenindo perdas."
    )
elif (
    indice_execucao >= 75
    and indice_perda <= 20
):
    nivel = "Atenção"
    simbolo = "🟡"
    mensagem = (
        "O desempenho está razoável, mas existem perdas "
        "que podem ser reduzidas."
    )
else:
    nivel = "Crítico"
    simbolo = "🔴"
    mensagem = (
        "Há perda relevante do planejamento. "
        "Priorize No-show, improdutivas e pendências futuras."
    )

return {
    "Planejadas": planejadas,
    "Executadas agendadas": executadas_agendadas,
    "Executadas extras": executadas_extras,
    "Executadas totais": (
        executadas_agendadas
        + executadas_extras
    ),
    "Improdutivas": (
        improdutivas_agendadas
        + improdutivas_extras
    ),
    "Improdutivas agendadas": improdutivas_agendadas,
    "Improdutivas extras": improdutivas_extras,
    "No-show": no_show,
    "Canceladas": canceladas,
    "Índice de execução": indice_execucao,
    "Índice de perda": indice_perda,
    "Nível": nivel,
    "Símbolo": simbolo,
    "Mensagem": mensagem,
}

def carregar_planejamento_futuro_portal() -> pd.DataFrame:
"""
Planejamento futuro de TODOS os tipos de serviço da oficina.
Follow continua sendo exclusivo para manutenções.
"""
registros = buscar_todos(
"atividades_planejadas",
ordem="data_operacional",
desc=False,
)

if not registros:
    return pd.DataFrame()

linhas = []

for registro in registros:
    dados_json = dict(
        registro.get("dados")
        or {}
    )

    cliente_nome = (
        texto_limpo(
            registro.get("cliente", "")
        )
        or texto_limpo(
            dados_json.get("Cliente", "")
        )
    )

    local_servico = (
        texto_limpo(
            registro.get("cidade", "")
        )
        or texto_limpo(
            dados_json.get("Cidade", "")
        )
        or texto_limpo(
            dados_json.get("Local", "")
        )
        or texto_limpo(
            dados_json.get("Localidade", "")
        )
    )

    linha = {
        "Data": str(
            registro.get(
                "data_operacional"
            )
            or ""
        ),
        "OS": texto_limpo(
            registro.get("os", "")
        )
        or texto_limpo(
            dados_json.get("OS", "")
        ),
        "Cliente": cliente_nome,
        "Local": local_servico,
        "Oficina": texto_limpo(
            registro.get("oficina", "")
        )
        or texto_limpo(
            dados_json.get("Oficina", "")
        ),
        "Tipo de Serviço": texto_limpo(
            registro.get(
                "tipo_atividade",
                "",
            )
        )
        or texto_limpo(
            dados_json.get(
                "Tipo de Atividade",
                "",
            )
        ),
        "Status": texto_limpo(
            registro.get(
                "status_atividade",
                "",
            )
        )
        or texto_limpo(
            dados_json.get(
                "Status da Atividade",
                "",
            )
        ),
        "Ativa": bool(
            registro.get(
                "ativa_no_planejamento",
                True,
            )
        ),
    }

    linhas.append(linha)

df = pd.DataFrame(
    linhas
)

if df.empty:
    return df

df["Data_dt"] = pd.to_datetime(
    df["Data"],
    errors="coerce",
).dt.date

hoje = date.today()

df = df[
    df["Data_dt"].notna()
    & (df["Data_dt"] >= hoje)
    & (df["Ativa"] == True)
].copy()

df = df[
    df["Oficina"].apply(
        normalizar_texto
    )
    == normalizar_texto(
        OFICINA_PORTAL
    )
].copy()

df = df[
    ~df["Status"].apply(
        status_cancelado
    )
].copy()

return df







=========================================================
DETALHAMENTO CLICÁVEL DOS INDICADORES DO PORTAL
=========================================================

def definir_detalhe_portal(filtro: str) -> None:
st.session_state["portal_detalhe_ativo"] = filtro

def limpar_detalhe_portal() -> None:
st.session_state["portal_detalhe_ativo"] = None

def filtrar_detalhe_portal(base: pd.DataFrame, filtro: str) -> pd.DataFrame:
classes = base["Classificação"].fillna("").astype(str)

mapa = {
    "Planejados": [
        "Executada agendada",
        "Improdutiva agendada",
        "Cancelada",
        "No-show",
        "Status intermediário agendado",
    ],
    "Executados": [
        "Executada agendada",
        "Executada extra",
    ],
    "Não concluídos": [
        "Improdutiva agendada",
        "Improdutiva extra",
    ],
    "No-show": ["No-show"],
    "Cancelados": ["Cancelada"],
    "Execuções extras": ["Executada extra"],
}

classes_filtro = mapa.get(filtro, [])
return base[classes.isin(classes_filtro)].copy()

def exibir_card_portal(coluna, titulo: str, valor, filtro: str | None = None) -> None:
coluna.metric(titulo, valor)

if filtro:
    coluna.button(
        "🔎 Ver OS",
        key="portal_ver_" + normalizar_texto(filtro).lower().replace(" ", "_"),
        on_click=definir_detalhe_portal,
        args=(filtro,),
        use_container_width=True,
    )

def exibir_detalhe_portal(base: pd.DataFrame) -> None:
filtro = st.session_state.get("portal_detalhe_ativo")
if not filtro:
return

detalhe = filtrar_detalhe_portal(base, filtro)

st.markdown("---")
st.subheader(f"🔎 Conferência das OS — {filtro}")
st.caption(
    f"Foram encontrados {len(detalhe)} atendimento(s) "
    "da oficina no período selecionado."
)

colunas = [
    "Data Operacional",
    "Tipo de Serviço",
    "Classificação",
    "Ticket",
    "Placa",
    "OS_planejada",
    "OS_resultado",
    "Status_planejado",
    "Status_resultado",
    "Razao_improdutiva",
    "Observacao_tecnico_improdutiva",
    "Motivo da Classificação",
]
colunas = [c for c in colunas if c in detalhe.columns]

exibicao = detalhe[colunas].copy().rename(
    columns={
        "OS_planejada": "OS planejada",
        "OS_resultado": "OS resultado",
        "Status_planejado": "Status planejado",
        "Status_resultado": "Status resultado",
        "Razao_improdutiva": "Razão da Improdutiva",
        "Observacao_tecnico_improdutiva": "Observação do Técnico",
    }
)

st.dataframe(
    exibicao,
    use_container_width=True,
    hide_index=True,
    height=500,
)

if st.button(
    "✖ Fechar",
    key="portal_fechar_detalhe",
):
    limpar_detalhe_portal()
    st.rerun()
=========================================================
PORTAL YESHUA — MVP 1.0 (SOMENTE LEITURA)
=========================================================

st.title("🏢 YESHUA RASTREAMENTO")
st.caption("Portal Operacional • Planejamento, execução e Follow")

st.info(
"Portal piloto com Follow automático. As manutenções planejadas importadas "
"pela gestão aparecem automaticamente para confirmação da oficina."
)

try:
bases = listar_bases()
except Exception as exc:
st.error(f"Não foi possível consultar as bases operacionais: {exc}")
st.stop()

if bases is None or bases.empty:
st.warning("Ainda não existem bases operacionais disponíveis.")
st.stop()

Gera/atualiza automaticamente as pendências de Follow a partir
das manutenções planejadas da própria oficina.

resultado_follow_automatico = {
"criados": 0,
"atualizados": 0,
"pendentes": 0,
"datas_detectadas": [],
"os_detectadas": 0,
}

try:
resultado_follow_automatico = (
gerar_follows_automaticos_portal()
)
except Exception as erro_follow:
st.warning(
"Os indicadores estão disponíveis, mas não foi possível "
f"atualizar o Follow automático neste momento: {erro_follow}"
)

datas_planejado = set(
bases.loc[bases["tipo"] == "planejado", "data_operacional"].astype(str)
)
datas_resultado = set(
bases.loc[bases["tipo"] == "resultado", "data_operacional"].astype(str)
)
datas_completas = sorted(datas_planejado & datas_resultado)

if not datas_completas:
st.warning("Ainda não há datas com Planejado + Resultado disponíveis.")
st.stop()

datas_dt = pd.to_datetime(pd.Series(datas_completas), errors="coerce").dropna()
data_max = datas_dt.max().date()
data_min = datas_dt.min().date()

inicio_padrao = max(data_min, data_max.replace(day=1))

with st.sidebar:
st.header("Meu desempenho")
periodo = st.date_input(
"Período",
value=(inicio_padrao, data_max),
min_value=data_min,
max_value=data_max,
key="portal_periodo",
)
st.caption("Oficina vinculada")
st.success(OFICINA_PORTAL)
st.divider()
st.caption("Portal da Oficina • MVP 1.4.0")

if isinstance(periodo, (tuple, list)) and len(periodo) == 2:
inicio, fim = periodo
else:
inicio = fim = periodo[0] if isinstance(periodo, (tuple, list)) else periodo

datas_periodo = [
d for d in datas_completas
if inicio <= pd.to_datetime(d).date() <= fim
]

if not datas_periodo:
st.info("Não existem bases completas no período selecionado.")
st.stop()

with st.spinner("Atualizando indicadores da oficina..."):
consolidado = carregar_consolidado_portal_todos_servicos(datas_periodo)
cadastro = carregar_oficinas()
if not cadastro.empty:
consolidado = enriquecer_com_cadastro(consolidado, cadastro)

if consolidado.empty:
st.info("Não há atendimentos consolidados nesse período.")
st.stop()

col_oficina = "Oficina"
if col_oficina not in consolidado.columns:
st.error("A base consolidada não contém a coluna de Oficina.")
st.stop()

dados = consolidado[
consolidado[col_oficina].fillna("").astype(str).str.strip().str.upper()
== OFICINA_PORTAL.upper()
].copy()

Série de classificações usada nas análises e abas do portal.

classes = dados["Classificação"].fillna("").astype(str)

if dados.empty:
st.warning(
"Nenhum atendimento da YESHUA RASTREAMENTO foi localizado no período. "
"Confira se o nome da oficina no cadastro/base está exatamente vinculado."
)
st.stop()

Indicadores do Portal: todos os tipos de serviço.

indicadores = calcular_indicadores_portal_todos_servicos(
dados
)

planejadas = indicadores["Planejadas"]
executadas_ag = indicadores["Executadas agendadas"]
executadas_extra = indicadores["Executadas extras"]
executadas = indicadores["Executadas totais"]
improdutivas = indicadores["Improdutivas"]
improd_ag = indicadores["Improdutivas agendadas"]
improd_extra = indicadores["Improdutivas extras"]
no_show = indicadores["No-show"]
canceladas = indicadores["Canceladas"]
indice_execucao = indicadores["Índice de execução"]
indice_perda = indicadores["Índice de perda"]

st.subheader(
f"Seu desempenho • "
f"{inicio.strftime('%d/%m/%Y')} a "
f"{fim.strftime('%d/%m/%Y')}"
)

st.markdown(
f"### {indicadores['Símbolo']} "
f"Termômetro operacional: {indicadores['Nível']}"
)
st.caption(
indicadores["Mensagem"]
)

c1, c2, c3, c4 = st.columns(4)

exibir_card_portal(c1, "Serviços planejados", planejadas, "Planejados")
exibir_card_portal(c2, "Executados", executadas, "Executados")
exibir_card_portal(c3, "Não concluídos", improdutivas, "Não concluídos")
exibir_card_portal(c4, "No-show", no_show, "No-show")

c5, c6, c7, c8 = st.columns(4)

exibir_card_portal(c5, "Cancelados", canceladas, "Cancelados")
exibir_card_portal(c6, "Execuções extras", executadas_extra, "Execuções extras")
exibir_card_portal(c7, "Índice de execução", f"{indice_execucao:.1f}%")
exibir_card_portal(c8, "Índice de perda", f"{indice_perda:.1f}%")

st.caption(
"Índice de execução = executados do planejamento ÷ "
"(planejados − cancelados). "
"Índice de perda = improdutivas agendadas + No-show ÷ "
"(planejados − cancelados)."
)

exibir_detalhe_portal(dados)

Planejamento futuro de todos os tipos de atividade.

planejamento_futuro = (
carregar_planejamento_futuro_portal()
)

if not planejamento_futuro.empty:
hoje = date.today()
fim_semana = hoje + timedelta(
days=6
)

proximos_7 = planejamento_futuro[
    planejamento_futuro["Data_dt"]
    <= fim_semana
].copy()

if not proximos_7.empty:
    st.info(
        f"📅 Você possui **{len(proximos_7)} serviço(s)** "
        "planejado(s) para os próximos 7 dias. "
        "O objetivo é converter o máximo possível desse planejamento."
    )

    with st.expander(
        "Ver planejamento dos próximos 7 dias"
    ):
        st.caption(
            "Cada OS aparece separadamente para facilitar "
            "a análise de cada atendimento."
        )

        colunas_planejamento = [
            "Data",
            "OS",
            "Cliente",
            "Local",
            "Tipo de Serviço",
            "Status",
        ]

        colunas_planejamento = [
            coluna
            for coluna in colunas_planejamento
            if coluna in proximos_7.columns
        ]

        st.dataframe(
            proximos_7[
                colunas_planejamento
            ].sort_values(
                ["Data", "OS"]
            ),
            use_container_width=True,
            hide_index=True,
            height=420,
        )

st.divider()

qtd_follows_pendentes, qtd_os_follow_pendentes = (
resumo_alerta_follow_portal()
)

rotulo_follow = (
f"🔴 Follow ({qtd_follows_pendentes})"
if qtd_follows_pendentes
else "✅ Follow"
)

aba_resumo, aba_improd, aba_noshow, aba_os, aba_follow = st.tabs([
"📊 Resumo",
"🔴 Não concluídos",
"🚫 No-show",
"🔎 Serviços",
rotulo_follow,
])

if qtd_follows_pendentes:
st.warning(
f"⚠️ Você possui {qtd_os_follow_pendentes} manutenção(ões) "
f"em {qtd_follows_pendentes} data(s) aguardando confirmação "
"de Follow. Acesse a aba Follow e responda."
)

datas_planejamento_detectadas = (
resultado_follow_automatico.get(
"datas_detectadas",
[],
)
)

if datas_planejamento_detectadas:
datas_formatadas = ", ".join(
pd.to_datetime(data).strftime("%d/%m/%Y")
for data in datas_planejamento_detectadas
)

st.caption(
    "Planejamento futuro detectado para a oficina: "
    f"**{datas_formatadas}** · "
    f"{resultado_follow_automatico.get('os_detectadas', 0)} "
    "manutenção(ões) vigente(s)."
)

with aba_resumo:
st.markdown("### Serviços por tipo")

if "Tipo de Serviço" in dados.columns:
    tipos_resumo = (
        dados[
            "Tipo de Serviço"
        ]
        .fillna("Não informado")
        .astype(str)
        .value_counts()
        .rename_axis(
            "Tipo de Serviço"
        )
        .reset_index(
            name="Quantidade"
        )
    )

    st.dataframe(
        tipos_resumo,
        use_container_width=True,
        hide_index=True,
    )

st.markdown("### Evolução diária")
diario = (
    dados.groupby(["Data Operacional", "Classificação"])
    .size()
    .reset_index(name="Quantidade")
)
fig = px.bar(
    diario,
    x="Data Operacional",
    y="Quantidade",
    color="Classificação",
    barmode="stack",
)
st.plotly_chart(fig, use_container_width=True)

st.markdown("### Distribuição dos resultados")
resumo = (
    classes.value_counts()
    .rename_axis("Classificação")
    .reset_index(name="Quantidade")
)
st.dataframe(resumo, use_container_width=True, hide_index=True)

with aba_improd:
imp = dados[
classes.isin(["Improdutiva agendada", "Improdutiva extra"])
].copy()

st.markdown(
    f"### {len(imp)} serviço(s) não concluído(s) no período "
    f"• {improd_ag} agendada(s) • {improd_extra} extra(s)"
)

if imp.empty:
    st.success("Nenhuma improdutiva encontrada no período.")
else:
    motivo_col = "Razao_improdutiva"
    obs_col = "Observacao_tecnico_improdutiva"

    if motivo_col in imp.columns:
        motivos = (
            imp[motivo_col]
            .fillna("Não informado")
            .astype(str)
            .value_counts()
            .rename_axis("Motivo")
            .reset_index(name="Quantidade")
        )
        fig_m = px.bar(
            motivos.head(10),
            x="Quantidade",
            y="Motivo",
            orientation="h",
            text="Quantidade",
        )
        st.plotly_chart(fig_m, use_container_width=True)

    cols = [
        c for c in [
            "Data Operacional", "Tipo de Serviço", "Classificação",
            "Ticket", "Placa", "OS_planejada", "OS_resultado",
            motivo_col, obs_col
        ] if c in imp.columns
    ]
    st.dataframe(imp[cols], use_container_width=True, hide_index=True)

with aba_noshow:
ns = dados[classes == "No-show"].copy()
st.markdown(f"### {len(ns)} no-show(s) no período")

if ns.empty:
    st.success("Nenhum no-show encontrado no período.")
else:
    cols = [
        c for c in [
            "Data Operacional", "Tipo de Serviço", "Ticket", "Placa",
            "OS_planejada", "OS_resultado", "Consultor", "UF-base"
        ] if c in ns.columns
    ]
    st.dataframe(ns[cols], use_container_width=True, hide_index=True)

with aba_os:
st.markdown("### Detalhamento operacional")
tipos = sorted(classes.dropna().unique().tolist())
filtro_tipo = st.multiselect(
"Classificação",
tipos,
key="portal_filtro_classificacao",
)

detalhe = dados.copy()
if filtro_tipo:
    detalhe = detalhe[detalhe["Classificação"].isin(filtro_tipo)]

cols = [
    c for c in [
        "Data Operacional", "Tipo de Serviço", "Classificação",
        "Ticket", "Placa", "OS_planejada", "OS_resultado", "Troca de OS",
        "Razao_improdutiva", "Observacao_tecnico_improdutiva"
    ] if c in detalhe.columns
]
st.dataframe(
    detalhe[cols].sort_values("Data Operacional", ascending=False),
    use_container_width=True,
    hide_index=True,
    height=560,
)

with aba_follow:
exibir_follow_portal()

st.divider()
st.caption(
"Piloto YESHUA RASTREAMENTO • Dados provenientes da base operacional PS. "
"Portal com Follow automático por planejamento."
)

Fechar
